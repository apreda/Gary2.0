# September 19: repeated failure investigation

Adam's 3:02 PM game-pick and 3:05 PM Winners screenshots showed two different problems. All seven named college game tickets were saved before their noon kickoffs. Their first candidate records were created between 11:32 and 11:55 AM ET. A failed database coverage read discarded the collector's evidence of publication and revived old scheduler failures. This also recurred around 4:10 PM; those incidents cleared at 4:19 PM.

Winners prop comparison 65 really exceeded its ten-minute lease. It involved twelve candidates across eleven games, so one failed batch generated eleven incidents. Its second attempt completed at 3:10:58 PM; the incidents cleared at 3:11:42 PM. The original attempt's detailed error is not retained in that row, and the old worker errors lacked timestamps. Do not assert a proven cause for that first timeout. The database/API was unavailable around the same time.

## Live corrections

- The existing host-health report retains same-day, league-specific published game IDs across read failures. Successful reads replace that evidence; Eastern midnight clears it. Current read failures remain failures. Previously observed conference exclusions clear older attempts without suppressing a newer failure after the last known slate.
- Winners display mirroring reads only display fields: the live response measured 21,834 bytes instead of roughly 25 MB of unnecessary research. It performs one bulk write instead of a write for every game. Only the coverage loop owns this mirror.
- Reconciliation reads small receipt fields first (17,481 bytes for today's 102 candidates) and loads original desks only for missing/incomplete game records. Complete tickets are not repeatedly recovered and rewritten. Original evidence and published predictions remain immutable.
- Removed the daemon's obsolete per-ticket readers and historical release scans. Current daily game/prop curation and publication continue independently.
- Comparison time is limited by the actual database lease, with one minute reserved before expiry. Database claims have a 30-second response limit and finishes 15 seconds; only the same idempotent finish is retried. No extra model review or scheduled AI monitor was added. Logs now identify the run, attempt, lease and time; worker errors no longer dump multi-megabyte response bodies.

The canonical `com.gary.winners` worker restarted at 4:58:14 PM ET, PID 66729, after a database query confirmed zero active comparisons. The existing host-health/operations jobs read their updated scripts directly. No schema or edge function changed in that initial repair. Native build 2.26 (936) remains the current TestFlight delivery; these are server/worker changes.

## Still open

Postgres restarted at 4:36:45 PM and again 4:57:48 PM ET; earlier investigation also observed a 3:36:52 PM restart. The 4:57 outage produced HTTP 521 across all twelve coverage reads. The code changes remove substantial avoidable load, but memory exhaustion versus another server cause is unconfirmed. The connector lacks server logs and the browser dashboard is signed out. Adam explicitly deferred dashboard sign-in: **do the remaining work now; inspect those logs later**. Do not claim that recurring database restarts are solved or change paid compute without authorization.

At 4:58 PM there were 49/50 eligible NCAAF game picks, with none missing after kickoff, plus 34 distinct college props. Raw daily-pick totals also include earlier out-of-scope entries and are not the eligible coverage denominator. Missing BDL markets remain absent. Some starting-QB, availability and coaching source records remain unresolved, with specific reasons in `host-health-latest.json`.

The 4:33 PM Wire refresh failed for MLB: Claude subscription quota was exhausted and both GPT retrieval attempts produced no completed search receipts. NCAAF refreshed successfully in that pass. A single targeted Wire recovery was run during this investigation; its receipt is in the evidence folder. Do not treat a source-free model answer as retrieved news.

That recovery completed at 5:02:53 PM ET: one sourced MLB item and six NCAAF items published through the personal GPT subscription; the Wire stage is successful again. No paid Claude/OpenAI route was used.

## Additional live failure: MLB player-log window

Braves–Astros failed at 4:58:53 PM for a separate code defect. Both June player-log callers ignored `num_games`, despite the tool promising five by default and fifteen at most. Six recent-form hitter requests sent full seasons (including a 145-row Drake Baldwin season). One tool response reached 1,972,885 characters against the CLI's 1,048,576-character request limit. After fallback, accumulated research exceeded DeepSeek's context too. This was actual input bloat, not a missing sportsbook quote or a judgment about Gary's writing.

Both research and decision callers now use the same existing recent-game selection as the current player-log tool. It keeps every field and all batting/pitching rows belonging to each requested game, selects by actual dates, and retains relief appearances and zeroes. Full-season queries remain full-season. June's prompts, factors, checklist, decision order and opinions are unchanged. The two frozen-file pins change solely for this authorized data-window repair. A flow regression verifies a 145-game source becomes the requested five-game research sample and two-game decision sample, with complete row equality.

The fresh Braves–Astros recovery started at approximately 5:10 PM ET and completed successfully at 5:26 PM: **Braves ML +114**, game `5060089`, was saved in `daily_picks` with June era `224f81048bd5`. A direct database read at 5:26:42 PM confirmed the stored ticket, model and era; the row's update time was 5:26:19 PM. The process exited zero after all twelve research sections and the normal decision flow. Its single-run output is `braves-recovery.log`, and `database-final-receipt.json` contains the saved-record receipt. Future scheduler children load the repair directly; no scheduler interruption was required.

At 5:11 PM Postgres had remained up since 4:57:48. Fifty executions of the new display read averaged 6.29 ms, versus 579.26 ms for the old full-record query observed immediately before worker replacement. This is measured query improvement, not proof that the server restart cause is resolved. GitHub verification for the first fix commit `89f9b1dd` passed all three jobs. Production parity confirmed the running canonical worker, deployed functions and pushed main; only the three preserved unrelated working-tree changes keep its overall flag non-green.

The final 5:26:42 PM database receipt still showed the same 4:57:48 PM server start, about 29 minutes without another restart. Winners prop comparison **67**, created at 5:10:21 PM under the updated worker, completed on its first attempt at **5:11:17 PM** with no error. The latest three comparisons were all completed. These are live recovery receipts; the underlying server restart diagnosis remains deferred.

Verification: after the final scope-memory and player-log changes, the complete backend suite passed 409 files / 4,388 checks. GitHub run [35469680261](https://github.com/apreda/Gary2.0/actions/runs/35469680261) for `095503c3` passed backend/edge, web/types and Apple checks. These are development regressions, not an AI approval layer for picks. Evidence: `/Users/adam.preda/Documents/ChatGPT/Gary/backend-failures-2026-09-19/`. Preserve unrelated `deno.lock`, the private uncommitted GoogleService plist and the NFL audit folder.

## Follow-up: 2:31 PM host / X schedule screenshot

The two collector recovery entries at 1:28:52 PM followed read timeouts one minute earlier. They do not establish that model or prop generation itself failed. The 2:31 PM host-report incident cleared at **2:32 PM**, and the X schedule incident cleared at **2:35 PM**. All four pictured incident types were resolved when checked at 5:29 PM; the Mac's latest successful report was 5:29:18 PM.

The X incident exposed another real defect. Scheduled runs at **2:00, 2:05 and 2:10 PM**, and again at **5:05 PM**, failed with `duplicate key value violates unique constraint social_requests_pkey`. `gary_ops.enqueue_social()` inserted a pg_net request number into a durable lookup table. Production's pg_net queue, responses and request-number sequence are all unlogged; after crash recovery, request numbers can repeat. The old lookup row survived and rejected the new number, rolling back the transaction containing the HTTP enqueue. This monitoring mistake therefore blocked actual scheduled publishing requests.

Migration **20260919213333_align_social_request_lifetime_with_pg_net** was applied to production at approximately **5:33 PM**. It changes only `gary_ops.social_requests` to UNLOGGED, matching the lifetime of the pg_net data it tracks. [PostgreSQL documents the shared crash-reset behavior](https://www.postgresql.org/docs/current/sql-createtable.html#SQL-CREATETABLE-UNLOGGED). Publication receipts, incident/event history, email history and RLS remain durable/unchanged. The nine focused database cases passed, including an actual immediate shutdown/restart of the isolated local fixture database: the reused request ID successfully enqueues, old transient responses disappear, durable receipts/events survive, and the new HTTP success is observed correctly. Production was **not** restarted for verification.

The CLI dry run found pre-existing remote/local migration-history gaps, so no bulk history repair or unrelated migration was attempted. The table migration and its cleanup below were applied through MCP, and both local filenames match their recorded remote versions. The security advisor's [no-policy notice](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy) for the private `gary_ops` schema is intentional: public roles must not read the operations tables. Live catalog verification confirmed that `social_requests` alone is now unlogged and retains RLS; incidents, events and mail remain logged.

The table conversion preserves its current contents. Migration **20260919213627_prune_completed_pre_restart_social_lookups** removed **835 already-checked lookup rows from before the 4:57:48 PM restart**, retaining all seven current-cycle lookups. There were zero unchecked pre-restart rows. This removes existing collision candidates immediately, while the storage change prevents them after future crash recovery. Publication receipts and incident history were not deleted.

The **5:35 PM scheduled X run succeeded**, returned HTTP 200 and reported one posted pick, zero failed posts and zero missed picks. The host reported again at **5:36:23 PM**. The live receipt is `social-schedule-recovery.json` in the evidence folder. No extra publishing call, new schedule or AI monitor was added; the existing scheduled run supplied this verification.

## Follow-up: 4:50 PM coaching and 4:59 PM MLB screenshot

The pictured Braves–Astros failure is the real player-log overflow described above; its recovery published at **5:26 PM**. Do not regenerate that prediction or describe the original failure as an alert-only issue.

The college coaching incident was another real parser defect. The retrieved reports already contained Georgia Tech's current coaches, but the official staff source used season `2026-27`. Converting that label with `Number()` rejected it. Boise State and South Dakota's retrieved official staff directories explicitly named the 2026 season in their titles but omitted optional `kind` and `season` fields. The earlier staff-directory fix only accepted the exact tagged numeric format, and the prompt's JSON example omitted those tags. It therefore missed normal, already-observed provider output. Accepted staff sources were also incorrectly listed as rejected in diagnostics because comparison used object identity after cloning them.

The parser now understands ordinary academic-year labels and can establish the season from an explicitly labelled football staff-directory title when the optional fields are absent. The JSON example includes the requested tags. Ordinary news articles retain the dated-source rules; a staff directory still cannot establish a starting quarterback or injury status. Prior-year, unlabelled and non-directory examples remain rejected. Official source checks: [Georgia Tech](https://ramblinwreck.com/sports/m-footbl/roster), [Boise State](https://broncosports.com/sports/football/coaches), [South Dakota](https://goyotes.com/sports/football/coaches).

A one-time replay restored ten already-sourced coaching entries across those three schools in games **457273** and **457972**. It used the existing raw reports, preserved their original observation times and normal expiry, and required QB, injury, availability and other context fields to remain identical. Only the parsed context cache and four coaching-health rows were updated. No prediction was rewritten and no new model/search call, review layer or schedule was introduced. The normal research formatter now includes those coaches. Originals and receipts are saved in the existing evidence folder as `coaching-cache-before.json`, `coaching-context-receipt.json` and `coaching-coverage-receipt.json`.

The live report at **5:50:56 PM ET** confirmed **50/50 eligible games have coaching data for both teams**. The existing incident collector resolved the coaching incident at **5:51:36 PM**. Unresolved QB competitions and two availability-source gaps remain separate and visible; this receipt does not claim every college component is complete. Postgres was still running from its 4:57:48 PM start at the last check; the restart diagnosis remains deferred as requested.

Verification for the coaching correction: the four focused college modules passed all 37 cases, and the relevant complete backend run passed **409 files / 4,397 checks** at 5:53 PM. The only emitted compiler notice was the existing optional-string interpolation warning in the native historical-context fixture. Full output is `coaching-backend-checks.log` in the evidence folder.

## Follow-up: 7:01 PM Fresno State–San José State alert

This game (**457975**, kickoff **11 PM ET**) really had no published pick when checked at 7:40 PM. Its first scheduled attempt began at 7:00:23 PM and ended at 7:00:35 PM because `footballMarketUnavailable` found no priced spread. Business GPT was capped, but personal GPT's Sol preflight succeeded; model access did not cause this failure. Postgres had not restarted again after 4:57:48 PM.

A fresh exact-game BDL odds request at **7:40:44 PM** returned **zero rows**. A same-request comparison at **7:41:49 PM** returned zero rows for Fresno–San José State and four for Montana–Oregon State (**457974**). The morning `daily_slate` row has no spread, moneylines, total or vendor, and `odds_snapshots` contains zero records for Fresno's game. This establishes missing provider market data, not a quote dropped by our normalizer. BDL's underlying reason for omitting it is unknown. Do not fabricate a number, substitute another game's quote or claim the pick is recovered. The existing next retry is **8 PM ET**, followed by 9:30 and 10:30 PM.

The alert lost this specific reason because the scheduler only passed `Exit code 1`, while the runner persisted `required_data_unavailable` failures but omitted `market_unavailable`. The runner now records both in the existing incident files, and the existing collector renders a safe, specific missing-spread-and-price explanation. The actual original failure was recorded once with its original 7:00:35 PM timestamp so the current incident can show the reason too. No additional schedule, model review or odds fallback was added. The misleading log claiming every matched slate row was a verified market fallback now says only that available saved fields were merged and missing prices remain unavailable.

Evidence in the existing folder: `fresno-failed-run.log`, `fresno-market-receipt.json`, `fresno-alert-preview.json`. The four focused runner/incident/football-market modules passed all **42 cases**. The provider gap remains open until a real market arrives and a pick is durably published.

The normal collector updated the existing live incident with the specific missing-market explanation by **7:44:32 PM**, keeping it open. At **7:41:25 PM**, coverage was **15/15 MLB** and **49/50 eligible NCAAF**, with zero games started without a saved pick. The complete backend run passed **409 files / 4,398 checks**; the final classification adjustment also passed the full 15-case incident module. `fresno-backend-checks.log` holds the full run output. No public ticket was created or altered during this investigation.

## Follow-up: 7:42 PM Wire refresh alert

The 7:30 PM content run refreshed MLB successfully at 7:31:38 PM (four items), but failed NCAAF at **7:32:09 PM**. College's six earlier items from 5:02 PM remained available. Claude and business GPT were capped; personal GPT completed the response, but the shared search adapter rejected it because it contained no completed web retrieval.

The original personal-GPT transcript established the actual bug: it returned six `moment` updates using the verified finals and recap notes already in its input, including Flora's 194 rushing yards, Utah's 33–0 shutout, Howard's 115 receiving yards and Joseph's three passing touchdowns. These did not need a second source lookup. Wire's own prompt explicitly restricted game moments to the supplied notes, yet the shared adapter required a web search for every response. That redundant requirement prevented valid completed copy from reaching publication. `wire-rejected-supplied-facts.json` preserves the original input game notes and output without unrelated session data.

Wire now identifies whether it supplied actual recap records with notes. For that case, its existing subscription call can finish without web retrieval. Existing per-item behavior still links game moments to supplied notes and requires captured source URLs for outside reporting. Other research callers still require retrieval by default. No extra AI review or search retry was added. The Wire model setting is now forwarded properly; it had silently used Terra despite declaring Sol. Obsolete comments describing paid Anthropic search were removed.

The existing requested last-resort DeepSeek route can also write from supplied facts if every subscription fails; it is still excluded when outside source retrieval is required. Its generated text is never treated as a web receipt. No DeepSeek call was needed for this recovery.

A single normal `--stages wire` recovery completed at **7:59:01 PM ET**, using Sol on the personal GPT subscription: **five MLB items** (four supplied-recap moments and one source-linked news item) and **six NCAAF moments** were stored in `wire_items`. All other content stages and game predictions were untouched. The 7:59:45 PM host report shows both Wire feeds healthy and no failed `content-stages` observation. Evidence: `wire-evening-recovery.log`, `wire-evening-health-receipt.json` and `wire-evening-backend-checks.log` in the existing folder.

The full backend run passed **409 files / 4,403 checks** after the Wire/adapter correction. The final seven focused modules passed **48 cases**, including default retrieval enforcement, supplied-recap publication, outside-news source requirements and DeepSeek ordering/no-spend while a subscription succeeds.

The existing collector resolved `coverage:content-stages` at **8:00:47 PM ET**. An anonymous read through the app's public Supabase interface returned HTTP 200 with all **five MLB and six NCAAF items** at **8:01:41 PM** (`wire-publication-receipt.json`). After the final shared-route change, its complete dependent checks passed **64 files / 649 cases** (`wire-routing-checks.log`).
