# September 5 launch reliability audit

Recovery and source verification are complete; see the deployment section for remaining host/upload checks. Canonical source: `/Users/adam.preda/Desktop/Gary2.0`, main. Preserve the intentional uncommitted real Firebase plist. No injury-label, duration, or scout-rule changes were authorized here. Game model remains Astra; prop model remains Sol; confidence remains the model’s own judgment.

## Why the morning output was incomplete

The 7 AM Claude routine is an observer, not the generator. Its old instructions treat any fresh insight as coverage and recommend a scheduler restart without identifying the owning stage. The actual generation jobs start earlier. The September 5 board incident was repaired separately in `HANDOFF_2026-09-05_BOARD_RECOVERY.md`.

The largest additional cause was the shared football data limiter. It still reserved only three requests per minute for an old trial allowance. Normal authenticated NFL and NCAAF responses on September 5 both reported `X-RateLimit-Limit: 600`. Production now explicitly configures `GARY_BDL_LOCAL_REQUESTS_PER_MINUTE=120`, with 600 ms spacing including the safety margin; the fallback remains three for unconfigured environments. The shared lock, cancellation, and retries remain. See `gary2.0/docs/BDL_RATE_LIMIT.md`; sanitized header evidence is `/tmp/gary-bdl-rate-audit-2026-09-05.json`.

The NCAAF scheduler also used the next trigger for any college game as a child deadline. At 08:28 it killed healthy research for noon games because unrelated 12:30 games had an 08:30 trigger; two waiting noon games received zero runtime. NCAAF now uses three rolling workers, exact-game exclusion through game and props, and only its own retry/kickoff deadlines plus the existing 45-minute cap. A controlled reload at 08:45 activated the fixes (new scheduler PID 9699); captured old-process cleanup is recorded at `/tmp/gary-audit-scheduler-reload-2026-09-05.json`.

The content job was serial, could spend almost two hours reaching later stages, built cards twice, and kept only eight minutes for college cards. A single partial game pack marked that game finished for the day. The new existing-job runner gives stages owned process groups and bounded cancellation, records timestamped outcomes, prioritizes Home/Wire/cards, and separates insight generation from pack construction. A 02:30 football-card phase is installed on the same job, with a three-hour total ceiling before the normal morning runs. Cards checkpoint after each game, retry incomplete sides, and upsert without first deleting good data. Prior-season college rows have a longer cache lifetime; current-season windows remain short. NFL prior-season fallback is clearly labeled and applies only without a real current-season sample. A bounded `ncaaf-card-subjects` pass after college insights fills newly introduced subjects before Card Watch; 25 focused tests cover that final ordering change.

The existing scheduler watchdog now performs an independent read-only health check once between 07:00 and 08:00 Eastern, after its normal heartbeat check. Health never changes restart policy. The original Claude observer has not been edited because its management tool is unavailable. The new health command is `node scripts/morning-health.js --date YYYY-MM-DD`; it checks actual league/game coverage and distinguishes pending picks from games that started without picks.

An obsolete user crontab independently restarted the scheduler at 09:00 (PID 23440), interrupting healthy work. macOS unified logs confirm this was the clock-based cron, not the heartbeat watchdog. Removal is prepared but blocked by a native Full Disk Access authorization wait attributed to Codex; the existing entry remains installed. See `gary2.0/docs/MORNING_CONTENT_HEALTH.md` for exact evidence and guarded removal. Do not report this host issue fixed until installed readback proves it.

## App and data contracts repaired

- Hub card queries include league, exact player lookups are scoped by league, and card identities include league/game/player. Team matching normalizes verified MLB provider aliases (AZ/ARI, CWS/CHW, ATH/OAK) and uses the generated college team table when board abbreviations are absent. Ambiguous player names fail closed; accents and unique initials still resolve. Team sheets scope all supporting records to their league. Pull-to-refresh bypasses the card cache; empty early responses are not cached for 30 minutes.
- Team headline formats use constrained identity extraction rather than arbitrary substrings. College ranking matchup stories open their exact game. The actual Swift DTOs and routing methods were executed against 622 live cards and 124 signals: 64 player routes, 33 MLB team routes, and 11 college ranking routes resolved; 16 evidence-only stories correctly remain overlays. Houston’s 11 MLB cards and five college cards stay separate.
- Game-page player intel also scopes exact game IDs and matchup aliases by league, preventing NFL/NCAAF/MLB ID collisions.
- Web Today reads only NFL games on the exact Eastern date from the weekly row, excluding legacy daily duplicates. The August 29 fixture displays two games instead of all 16 weekly games.
- Native Billfold metadata now pages all 90 days with stable ordering and a new cache version. Public verification returned 1,286 rows instead of the truncated 1,000. A failed later page cannot overwrite a complete cache.
- Native results now exclude nine legacy NFL duplicates and normalize mixed-case grades. Actual Swift readers executed the complete public snapshot and match web at 1,742–1,531–8, +20.3u. Historical database rows remain intact. The final simulator UI independently displays the same total.
- Wire uses the existing Codex web transport instead of the unavailable Claude CLI chain, with bounded fallback and captured generation/source metadata. Unsupported market movement must not be inferred from aggregated web summaries. Recap runs now return a failing exit status when a requested recap fails instead of appearing successful with zero writes.

## Live evidence so far

- Home board: 45/45 games (15 MLB, 30 NCAAF). Tomorrow: 18 games.
- College card recovery started 08:47:43, saved 295 packs across all 30/30 games, and finished successfully 08:49:58. Every game’s latest checkpoint has both team IDs and a complete marker. Duration: 135 seconds, no 429 or roster/stat errors. Log: `/tmp/gary-ncaaf-card-recovery-2026-09-05.log`.
- MLB refresh completed at 09:09:21: 327 packs, 326 season sections, all 300 on-slate players with their exact game card and season section. Missing on-slate season sections fell from 200 to zero. Gavin Stone is the only thin off-slate card because the provider supplied no current-season stats or form. Log: `/tmp/gary-mlb-card-recovery-2026-09-05.log`.
- College insight recovery completed 09:22:36 through the new durable stage runner: 129 rows across 30 games, including 57 quarterback entries. Partial category refresh now preserves previously completed games. A subsequent 16.4-second targeted card pass at 10:19:56 added 18 newly named subjects: 313 college cards and all 82/82 named player rows matched by exact game/player. Thirteen added cards have verified roster identity/context without invented offensive statistics. All 30 two-team completion markers remain.
- Wire source audit retained five MLB and three college items. Unsupported claims were backed up and removed by exact ID. Availability had researched 12/30 games within its existing budget at 09:24; later scheduled passes can continue the rest.
- September 4: 13/13 game tickets graded and 13/13 recaps present. Earlier audit also verified all 33 stored props graded.
- Public browser checks: Home, Today, Picks, Props, Hub, Results, Winners, and Leaderboard load. Winners September 4 displays seven stored tickets. Leaderboard window/sort controls remain responsive, including clicking the selected control again. Its empty community result is legitimate.
- Native simulator build 900: Home loads board and recaps; Hub MLB bullpen team card opens Brooks Lee’s stats card; Athletics resolves ATH/OAK and shows its clubhouse; Conner Weigman opens the college QB card. Billfold loads its ledger and equity curve. Final build 900 also verifies the Houston college team card with five college players, without crossing into MLB Houston cards. Final Billfold displays +$2,030 and the canonical record.

## Verification and deployment

Initial full verification passed with two workers and isolated PostgreSQL enabled: 1,897 backend tests, 168 edge tests, 308 web tests, and Next/TypeScript checks. Full fixture web smoke passed. Logs: `/tmp/gary-launch-verify-20260905.log` and `/tmp/gary-launch-smoke-web-20260905.log`. Subsequent scoped fixes receive focused checks and final commit CI.

Final local full verify passed with isolated PostgreSQL and two workers: 1,954 backend, 168 edge, and 313 web tests plus Next/TypeScript. A test-only PID fixture publication race was fixed using atomic rename; no runtime behavior changed. The successful run had no source changes according to its 911-file manifest. Log: `/tmp/gary-launch-final-verify-rerun-20260905.log`.

Final build 2.25/900 simulator and Release archive succeeded from the internal SSD. The external KINGSTON archive attempt failed with disk I/O errors; its older archive must not be uploaded. Valid archive: `/tmp/GaryApp-Reliability-2.25-900.xcarchive`; log `/tmp/gary-reliability-900-archive-internal.log`. Upload and audit commit/deployment were subsequently verified below; cron removal remains blocked. A separate founder-requested quarterback display repair is being checked by the pick thread before upload; do not conflate archive success with TestFlight delivery.

Live health at 10:15:53 ET: board45/45, MLB15/15 and NCAAF30/30 game-card coverage, all13 grades/recaps, nine college picks published and no started games missing. MLB first pick window is14:40ET; a zero count this morning is expected.

Separate concurrent work: pick evidence/tool repair is committed as `28767e63` with live evidence documentation `12c88487`, followed by cross-sport stats repair `828c5e84`. Launch commits `1228452b` and `08b3c30c` were separately deployed and verified. Launch marketing/Book analytics changes belong to the separate launch thread and are excluded from this audit’s staging.

## Final deployment evidence

- Reliability commit `f97de41a` is on origin/main. CI `33972340278` passed, including the complete fixture browser smoke. Vercel production `dpl_F7ssg3tEaJdanCpCn4ZA6CytZp7c` is READY at that exact SHA with `www.betwithgary.ai` and `betwithgary.ai` aliases. Post-deploy Today displays all45 games,9 published calls and live Hub rows.
- Follow-up native commit `990e6041` fixes college game panels using exact league/provider game IDs when board abbreviations are absent; also prevents conflicting game IDs/doubleheaders sharing edges. Actual Swift regressions pass; CI `33972723221` passed. Final simulator rebuild succeeded.
- Production truth confirms one canonical scheduler PID23440, Winners PID11032, all edge functions deployed, game models Astra and props Sol. Stored early college picks use Astra with the versions active when generated. At that check the only dirty work was the intentional real Firebase configuration plus the separate DB repair thread.
- Founder explicitly authorized remote cron removal after saying he was away. Native CLI again stalled on macOS Full Disk Access; Terminal already has access, but the Computer Use tool explicitly rejects Terminal control for safety reasons. No privacy setting was changed and no pending cron process remains. Exact readback still contains the obsolete9AM restart. Guarded removal remains `/tmp/gary-remove-obsolete-scheduler-cron.py`.

- Final simulator verifies ORST–HOU now displays all seven stored edges (two quarterbacks, three availability rows, ranking and head-to-head), and the Hub-to-Picks link opens the college Picks page.
- Home incorrectly counted every daily game pick as a sealed Winners selection. `42e17e1c` removes that unverified count and schedule prediction; Home now says selections appear after review. The actual Winners page correctly has no admissions yet. The final archive must include this native commit before upload.

## Latest database availability incident — 10:55 ET

A fresh health command at14:55:27.390UTC failed all eleven REST reads with timeouts. This supersedes earlier healthy snapshots for current availability. The separate founder-requested DB I/O repair thread also observed MCP/REST/CLI connection timeouts after its bounded old cron-history cleanup; cause is not established. That task has stopped mutations and owns recovery. Root stopped additional DB probes and did not restart the scheduler. Do not claim current production health until recovery is observed. Failed check: `/tmp/gary-launch-health-verified-20260905.log`. Native archive work continues independently.

## Final native delivery — 11:03:46 ET

Final archive `/tmp/GaryApp-Reliability-2.25-900.xcarchive` includes native commits through `42e17e1c`. Xcode verified2.25/900 and uploaded it with the existing signing account. At11:03:46.777, Apple reported “Uploaded package is processing”; at11:03:46.785 it reported “Upload succeeded”, followed by “EXPORT SUCCEEDED”, exit0. Log: `/tmp/gary-reliability-900-upload.log`. This proves delivery and entry into processing, not TestFlight availability, App Review approval or App Store publication. The launch thread owns subsequent App Store Connect verification; its browser session still requires founder sign-in.

Final simulator Home now displays “DAILY REVIEW” and “Selections appear after review · games + props”, without falsely counting free daily picks as Winners admissions. CI for final native source `42e17e1c` is green (`33973128559`).

Current readiness is explicitly incomplete: Supabase normal requests last timed out and the DB recovery task temporarily unloaded `com.gary2.daily-insights`, retaining its installed plist for resumption after recovery. That task owns resuming it. Scheduler23440 and Winners11032 remain untouched. The obsolete9AMhostcron still requires the protected native removal. No OS privacy settings were changed; no cron update process is pending. Do not infer launch readiness from build delivery or earlier data snapshots.
