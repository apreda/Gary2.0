# Launch review and publication recovery — September 7

Current six-stream scorecard: [September 7 execution review](GaryMarketing/launch-2026-09/EXECUTION_REVIEW_2026-09-07.md). This continues the existing daily launch follow-up, not a new automation. Content and pilot preparation remain distinct from publication and real customer evidence.

## Delivered

- `c9c90859` implements durable per-game X publication reservations and confirmed-receipt reconciliation. Migration **20260907101656_durable_social_publication** is applied; `social-auto-post` **v102** is deployed with JWT verification. All 12 cloud files match the reviewed source. Source CI **34111025223** passed. No campaign or verification tweet was posted.
- Reservations serialize the unchanged 30-root daily cap, counting legacy logs and new intents once. Each external write is preceded by a conditional state transition. A confirmed root is stored independently of the post log and before a reply. Retry repairs missing logs from receipts without replacing timestamps or wiping confirmed replies. Prepared intents resume frozen copy. Uncertain sends remain visible and never blindly retry; old uncertain intents remain visible in readiness even beyond the seven-day automatic repair window.
- State/table access is service-only; RLS is enabled and anonymous/authenticated table/RPC access is denied. Security-advisor comparison introduces no new findings. The live original uniqueness constraint was constraint-backed; the first migration transaction rolled back, the fixture/migration were corrected, and the successful applied version matches the local filename.
- `08ec3322` fixes another defect found during the final full-day check: `getUpcomingGames` applied its six-hour age guard to authoritative daily snapshots. An evening refresh had removed two early finished September 6 games. Full-day snapshots now explicitly retain games on the exact requested day; ordinary upcoming-pick filtering is unchanged. Exact provider IDs confirmed Brewers–Reds and Braves–Phillies were real completed games.
- The normal fresh-process `run-daily-slate.js --date 2026-09-06` restored **18 games**, removed **zero** rows and completed successfully. A subsequent exact-identity comparison is stored in `previous-day-publishing-restored-2026-09-07.json`. The scheduler already uses fresh snapshot child processes, so this source change requires no daemon restart. No scheduler, Winners, cron or billing setting was changed here.

## Verification and evidence

- Initial root `npm run verify`: **2,148 backend + 180 edge + 349 web tests = 2,677**, plus TypeScript. Actual edge entrypoint passes `deno check`.
- Seven isolated PostgreSQL cases exercise permissions, immutable reservations, legacy/new deduplication, daily limits and competing clients for the final slot. Eleven publication cases cover overlapping calls, uncertain root/reply outcomes, lost receipt/log writes, recovery after game start, metrics-only repair and a slow claim crossing the deadline.
- Final schedule-focused run: **37 tests** across four suites. Final full backend rerun: **2,150 passed**. Source CI **34111614236** passed `08ec3322`, including the repository smoke checks. Full web tests/types were already green; no web source changed.
- The cloud dry run returned 200, zero posts, zero metrics refresh and `health: ok`. It ran outside posting hours and does not prove a real X write. No intents existed at the initial live schema read. Verify the first natural daytime reservation/root/reply/log lifecycle at the next review.
- September 6: **18 actual X hooks**, all safe verbatim under the revised selector; 15 MLB and three NCAAF. About 55–120 minutes pregame. Latest unequal-age snapshots: 9,607 summed component impressions, 54 profile clicks, observed roughly 8.75–18.25 hours after publication. No audience-lift or install inference.
- First completed calendar week (August 31–September 6 UTC), partial launch exposure: **8 consented sessions, 1 useful read, 3 observed browsers**. No mature seven-day return denominator. Manual Book milestones are zero; retained 30-day aggregate is **111 entries, zero manual entries/accounts/settlements**. No actual pilot observations or import requests exist.
- Database remained up since the September 6 Micro correction at **12:57:05 UTC**. At September 7 10:19 UTC, all **1,348** recorded cron executions since midnight UTC were successful. This is a bounded observation, not permanent reliability or complete downstream verification. Morning launch read had 24 retained scheduled-poster responses, no degraded outcomes. Private engagement drafts still date to August 20; existing provider credit remains a dependency.
- Apple review browser currently shows sign-in. Last verified receipt remains build **901 Waiting for Review**, September 5, automatic release on approval. Do not imply a fresh approval observation. The prior archive/privacy and account QA evidence remains in the September 6 review.

## Handling an uncertain X send

Read the reserved copy, exact game identity, stage and timestamps. A `root_sending` or `reply_sending` row does not establish success or failure. Obtain an authoritative X receipt matching Gary's account, exact content, timestamp and parent/thread identity. For a verified root, save its actual ID and creation timestamp while conditionally advancing `root_sending` to `root_sent`; for a verified reply, save the actual reply ID while advancing `reply_sending` to `reply_sent`. Subsequent reconciliation repairs the log. Never fabricate IDs, backdate a publication, infer nonexistence from a search miss or reset an uncertain state merely to trigger another send. No such manual reconciliation was needed in this review.

## Remaining work and shared checkout

Observe natural publishing and the next overnight window. Complete account/channel controls, actual prepared-content rollout and real-user pilot participation when the existing publication/contact dependencies are resolved. Do not create invitations, vendor messages, spending or new campaign posts from the daily heartbeat alone. Recheck eligible return cohorts and the October offer transition. Keep the protected obsolete 9 AM scheduler-cron question open; the other task's archived failure emails do not prove that cron was removed.

Preserved unrelated local changes: `gary2.0/scripts/lib/morningHealth.js`, its test file, and the machine's real `ios/GaryApp/GoogleService-Info.plist`. Do not stage or inspect the private plist. Source delivery is on canonical `main`, not the retired clone. Final production truth must be read with these explicit working-tree exceptions; source-parity evidence separately checks the actual deployed function contents.

The natural 06:30 Eastern scheduled invocation after v102 deployment returned HTTP 200, health `ok`, zero failed posts and zero posts outside publishing hours. No unresolved publication intents were present. This verifies the deployed scheduled entrypoint and recovery read, not a daytime X send.
