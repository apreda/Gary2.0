# Gary launch readiness — current release ledger

Updated September 8, 2026, 01:55 Eastern, after successful 910 upload and final production checks. This replaces the earlier running notes for the September 7 launch-readiness goal. Historical audit receipts remain in the evidence directory; superseded test counts and pending-source statuses are condensed here.

## Release decision

**NOT YET READY for launch signoff.** The core repairs through 909 are committed, the website is deployed, and native version 2.25 (909) is the verified, signed and successfully uploaded baseline. The subsequently confirmed P2 Hub label ambiguity is repaired in **2.25 (910)**, source `e88b70ce`; its optimized simulator build, signed archive and privacy/signature inventory pass. It uploaded successfully at **05:54:41.786 UTC / 01:54 ET**; Apple reports processing, with completion and build selection unverified. Guest simulator checks below apply to 909. Other release gates include Apple processing/build selection and metadata, physical-device and authenticated account flows, Supabase Auth settings, and actual moderation/support coverage.

A successful source test, guest simulator check, production deployment and physical-device check establish different facts. The table below records each separately. Nothing here claims App Store approval or that every possible app defect has been eliminated.

Canonical checkout: `/Users/adam.preda/Gary2.0`, `main`. Preserve the local private `ios/GaryApp/GoogleService-Info.plist` configuration exception; it is not part of review/staging. Starting source for this goal was `307d137a`.

## Current release artifacts and verification

| Artifact / check | Current verified state | Evidence / remaining boundary |
| --- | --- | --- |
| Website | **Committed, pushed and live:** `24b99c5c`, Vercel deployment `dpl_8jCMDFZw3aG6pkGcEb6x4orA8RQb`, status READY. Includes exact prop tracking, honest empty/error states and current launch/reading disclosures. | Full web run: **452 tests / 63 files**, types and credential-free real-app SSR smoke pass. Logs: `/Volumes/KINGSTON/gary-909-web-final.log`, `gary-909-web-types.log`, `gary-909-web-final-smoke.log`. Authenticated provider/customer flows are separate acceptance gates. |
| Native 909 verified baseline | **Committed and pushed:** `7e7c163a`, version **2.25 (909)**. Final optimized simulator build succeeded; **signed archive succeeded** at `/Volumes/KINGSTON/Gary-2.25-909-Launch.xcarchive`. | Signed archive inventory and strict/deep code signature verification passed. **Upload succeeded at 2026-09-08 05:42:00.321 UTC / 01:42 ET**, exit 0 / EXPORT SUCCEEDED; `/Volumes/KINGSTON/gary-909-upload.log`. Apple reports the uploaded package is processing; processing completion, group inclusion and selected submission build are not yet verified. |
| Native 910 narrow follow-up | **Committed and pushed `e88b70ce`**: **Team streaks** and an explicit **NEXT GAME** label, with wrapping. Five existing optimized Hub tests pass. Final optimized simulator build and signed archive pass. | Archive `/Volumes/KINGSTON/Gary-2.25-910-Launch.xcarchive`; logs `gary-910-simulator-build.log`, `gary-910-archive.log`. All 21 manifests / 14 categories match signed 909, root manifest matches source, strict/deep signature passes. **Upload succeeded 05:54:41.786 UTC**, exit 0 / EXPORT SUCCEEDED; `/Volumes/KINGSTON/gary-910-upload.log`. Final changed-label visual recheck is pending the already-requested Mac unlock. |
| Backend integrated regression | **289 files / 2,766 tests pass**, PostgreSQL 17 enabled, no skipped tests. | `/Volumes/KINGSTON/gary-909-backend-release.log`. External-disk AppleDouble metadata files are excluded; actual tests are not excluded. The last small Access copy/dock change was separately SDK-compiled and passed its actual Swift dock regression. |
| Native edge helpers | **217 tests pass**, unchanged by the final copy/dock work. | Parser, routing, registration and related helper evidence remains valid for its tested boundaries. |
| Final guest simulator UI | Verified on the optimized 909 build; final small-phone welcome/disclaimer/Picks entry and normal/maximum-text dock are visible. iPhone 17 dock also verified. | Page-specific evidence is recorded below and in `launch-readiness/909-ui-evidence/`. Simulator guest acceptance does not establish physical auth, live push, storefront billing or authenticated CRUD. |
| Previous 908 release | Version 2.25 (908) passed optimized build, signed archive and three cold launches; uploaded successfully at **2026-09-08 03:52:58 UTC**. | `/Volumes/KINGSTON/Gary-2.25-908-Responsiveness-final.xcarchive`. Do not upload it again. Processing/internal Beta inclusion were not proved because the App Store Connect session expired. 909 and then 910 superseded it as uploaded candidates; 910 is the current narrow follow-up. No verified change to the older 2.25 (901) App Store submission is recorded. |

The final website smoke verifies real Next rendering, result/export consistency and discovery endpoints against isolated fixture services. Browser checks additionally covered expanded fixture reasoning, the 1–1 results record, repeated leaderboard controls and honest qualification states. Neither check places real bets, creates fake standings or proves live account-provider behavior.

## Completed integrity and operational repairs

| Area | Implemented and deployed evidence | Limits of the evidence |
| --- | --- | --- |
| MLB game/prop settlement | `e4663ccf` repairs pitcher-versus-batter routing, strict count parsing, exact player/game identity, incomplete box evidence, game spread/total/push parsing and matching local/cloud behavior. 239 focused cases, isolated database contention checks and both Deno entrypoints passed. Deployed read-only checks inspected 12 game picks and 33 props / 24 computed, with no invalid identity, unavailable box or missing-stat failures. | No manual regrading or history correction was performed. Read-only dry checks do not mutate published picks or original rationale. |
| Historical reconciliation | **All 54 exposed pitcher-walk tickets** from August 8–September 7 matched final provider boxes: zero incorrect and zero unverified tickets. **All 428 game-results rows** were consistent with the new parser using stored ticket/matchup/final-score evidence. | The 428-row check is stored-evidence consistency, not an independent full provider refetch; it excludes the separate NFL results table. |
| Pregame prop persistence | Publication deadline, wall-clock enforcement after contention, immutable retry receipt comparison and exact ticket preservation are deployed. | Isolated contention/retry tests passed; production history was not rewritten. |
| Grading authorization | `1e731a4e` requires service authorization before mutable grading. Legitimate cron callers use Vault service credentials with original schedules/timeouts. 113 focused tests plus 25 actual handler/auth cases passed; deployed public mutation/preview calls return 403, service game-read-only/prop-dry calls return 200. | Public read-only social evidence remains available. No live grades were triggered for QA. |
| New-pick and personal-result push | `b2c5b791` repairs exact source identity, current access logic, durable per-device delivery receipts, concurrency/retry behavior and service-only dispatch. `17155b3c` adds personal settlement destinations and accurate five-win-streak copy. | 24 handler/database and 12 transport/source cases passed for new-pick delivery; intercepted actual FCM orchestration covered two users/three devices and 22 personal-result edge cases. No QA notification was sent. Physical delivery remains unverified. |
| Device/account registration | `02f4121a` binds personal registration to authenticated device sessions and serialized native account changes. Live migration preserved 234 tokens / 232 active tokens and cleared unproved legacy personal account links. Older clients retain generic alerts; personal results resume after a current signed-in app registers. | Twelve isolated SQL cases passed. Private installations enforce RLS with no client reads. No QA device was registered in production. |
| X mutation authorization | `c357480c` secures ten mutation endpoints and four legitimate internal callers, preserving cron cadence/timeouts. 147 focused and 77 existing social cases, all ten Deno entrypoints and 36 reconciled auth/cron/view cases passed. | All ten deployed endpoints reject anon with 403; service requests with missing required inputs reach expected validation without X calls. No QA post, reply or deletion occurred; posting strategy was unchanged. |
| Cache writer authorization | `68065a27` secures live-scores and MLB lineup refresh with legitimate callers/cron cut over to Vault. Public writer calls return 403; public cache reads remain 200. | Focused actual handler checks and both Deno entrypoints passed. Native public reading was preserved. |
| Public analytics views | Five views now enforce source RLS under the caller and expose SELECT-only client grants; all five public REST samples remained 200. | Narrow hardening; no private user/experiment leak had been demonstrated before the fix. |
| Profile safety | `4b02b98c` and subsequent native/web releases add authenticated report/block/blocked-list controls, private report storage, service-only moderation and public rules/support links. Thirteen isolated SQL cases and 43 focused web cases passed. | Technical queues and permissions do not establish actual moderation staffing or response coverage; that remains open. |

Applied migration versions are the actual Supabase versions, including reconciled local filenames:

| Applied migration | Purpose |
| --- | --- |
| `20260908031714_enforce_prop_publication_deadline.sql` | Post-lock wall-clock check and service-only immutable prop publication |
| `20260908032715_reliable_pick_push_delivery.sql` | Private durable delivery receipts and retry claims |
| `20260908032716_secure_pick_notification_cron.sql` | Vault authorization; original active five-minute cadence |
| `20260908033025_secure_grading_cron.sql` | Vault service authorization for both unchanged grading schedules/timeouts |
| `20260908034216_secure_social_service_cron.sql` | Vault authorization; cron 3 remains active every 15 minutes with 120-second timeout |
| `20260908034218_secure_public_analytics_views.sql` | Caller RLS and client SELECT-only analytics view grants |
| `20260908034829_secure_cache_refresh_cron.sql` | Vault authorization for unchanged cache schedules/timeouts |
| `20260908035634_profile_safety_controls.sql` | Private reports, block controls and authenticated/service RPC boundaries |
| `20260908041241_authenticated_push_registration.sql` | Authenticated account/device binding and protected installation capabilities |

Credentials were stored in Vault without printing them or embedding them in repository SQL. Post-cutover observations of ordinary scheduled work returned 200 for both graders; scores reported 12 games/no errors; lineups reported 15 games/15 writes; the X scheduler returned its expected outside-posting-window response. These were observations of scheduled work, not QA-triggered mutations.

The earlier production-truth receipt confirmed canonical workers, correct Astra/Sol choices and **MLB 11/11 published with zero started games missing picks**. Later cache deployment and normal cron observations closed that receipt's two then-pending function checks. Final production truth at **05:54 UTC** on committed `0638894c` confirms canonical scheduler PID 79083 and Winners PID 89023, correct Astra games / Sol props, all 20 edge timestamp checks passing and no unpushed commits. Exit 1 is solely the one preserved private GoogleService-Info.plist local exception. Its calendar-day Sep 8 query reports 0/0 because that slate is not populated yet; a separate read-only 6 AM slate query at **05:55:18 UTC** confirms the active Sep 7 board is **11/11 MLB games published, zero started without a pick**. This is timestamp deployment parity plus the earlier deployed behavior checks, not a fresh comparison of every deployed source byte. Receipt: `/Volumes/KINGSTON/gary-910-production-truth.log`.

## Completed native and web behavior

- **Date and request ownership:** Home's full/rolling loads now reject obsolete date/account/request results, including cold 6 AM rollover and late enrichment. Winners anchors its header and prop grades to the accepted board date, refreshes the visible current-date board/results every 90 seconds, and checks rollover every 30 seconds without overriding manual history. Empty-slate phase handling distinguishes upcoming review, all-started final empty, unknown schedule and unavailable sources. All are integrated, compiled and covered by executable regressions.
- **Game identity and data contracts:** MLB lineups use exact game/date identity without a matchup fallback; weekly NFL aliases normalize correctly. Optimized Hub league routing preserves 200 groups / 800 correctly scoped outcomes. The starter-record/two-start-schedule Codable union restored the actual public sample from 250/264 to **264/264** decoded records while preserving complete cache equality.
- **Book and leaderboard:** Exact verified receipts, pregame lock checks, fractional/unknown kickoff handling, fun-lane eligibility, directory failure/retry, private manual CRUD/export, streaks, qualification, pagination and public privacy are implemented. Public profile repeat-window loading, first-load false-zero Book statistics and standalone personal qualification were repaired and deployed. The real empty leaderboard is valid min-five decided-call/opt-in behavior; `bookPreviewCast` remains false.
- **Web exact props:** Featured cards pass game/line/side identity; same-player doubleheader sign-in returns arm only the intended ticket; explicit confirmation remains required. Unknown time is visibly unavailable, kickoff is rechecked before writes, doubleheaders get separate correctly timed panels and same-game MLB HR remains grouped with core props. Required-feed failure no longer masquerades as an unpublished empty board. Historical Winners empty copy no longer promises today's reviews.
- **Auth, access and onboarding:** Correct first-launch Picks destination, accessible intro, selected-plan disclosures, founding wording, complete password-recovery callback flow, Settings navigation and modal close controls are integrated. Native typed push navigation, serialized authenticated registration and profile safety controls compile for the supported iOS 16 target and pass their focused execution fixtures.
- **Reading measurement:** A separate optional, off-default consent choice counts in-memory sessions and five continuously visible seconds of expanded original game/prop card reasoning on instrumented Home/Winners/Picks surfaces. Active tab, foreground, viewport and modal gates apply; it excludes collapsed previews and does not claim Hub/Fantasy/chat coverage or cross-launch retention. Payloads omit account, installation, notification, pick/player and reasoning identifiers/content. Upgrades do not broaden checkout consent. Timing, lifecycle, payload, viewport and consent cases pass; no live QA analytics events were sent.

Leaderboard, private Book, verified receipts, streaks, profiles, account access, deletion and Fantasy are real implemented features. Sportsbook account import and Talk to Gary are intentionally absent launch features, not dead controls to activate. Manual bookmaker metadata is not a FanDuel/DraftKings integration. Retired NHL/NCAAB and finished tournament content must not be reintroduced as current coverage.

## Actual 909 page and interaction acceptance

These rows distinguish observed guest UI from unverified authenticated/device boundaries. They replace the earlier blanket statement that every page was pending.

| Surface | Actual final-candidate evidence | Remaining boundary |
| --- | --- | --- |
| Fresh launch / disclaimer / intro | Final iPhone SE journey: welcome → disclaimer → Access Picks → correct **tab 3** → intro. Normal and maximum text size keep the final dock visible; iPhone 17 dock also checked. | Physical-device accessibility and full first-use journey remain separate. |
| Home / date context | Guest Home and The Daily Card inspected, with the final iPhone 17 title confirmed in saved accessibility evidence. Executed full/rolling ownership cases cover partial/failure/cancellation/account/6 AM boundaries. | Physical touch/VoiceOver and authenticated private summary; simulator source fixtures are not a real overnight production observation. |
| Winners / history / plans | Closed-slate guest board and its honest no-selection state observed. Empty-state, board-date, history-preservation and periodic-refresh helpers execute against shipping source. | Authenticated founding/paid/revoked-access UI and physical storefront behavior; October entitlement boundaries have isolated fixtures but are not observed live in October. |
| Picks / game / props | Settled game and prop cards, original reads and correct accepted-slate header observed. After midnight the rechecked header correctly displayed **TODAY SEP 7**, matching the 6 AM slate. The actual final team modal was visually checked: stored prices say **SAVED ODDS**, distinct from live score status. | Actual before/at/after kickoff account action, every sport/postponed case and physical accessibility need their own acceptance. |
| Expanded long reasoning | On the rebuilt 909 Cubs card, ordinary coordinate-tap expansion stayed aligned with full visible text width. Intrinsic vertical text sizing and removal of the expansion spring are retained. | Sky's AX-targeted click shifted the rail; its drag also failed to scroll ordinary Home. That distinguishes a tool-specific reproduction from a proved app touch/VoiceOver bug. Complete vertical reading, collapse and VoiceOver remain unverified; no speculative rail/gesture rewrite was made. |
| Hub / player / game detail | Guest Hub stories, team/player routes and expanded pitcher stats were observed. **OPP AVG** is corrected in source; the attempted final More Stats/header recheck was interrupted by the Mac locking, so that final visual check is unverified. Exact routing and optimized league/model cases pass. | Physical long-reading gestures/accessibility; final pitcher header recheck after the already-requested Mac unlock; repaired 910 next-opponent labels awaiting visual recheck below. |
| Fantasy | Implemented MLB/NFL briefing and controls remain covered by prior source/optimized fixtures. Final candidate includes honest missing-data copy. | Every interactive sport/format/filter and long-reasoning journey is not claimed fully exercised in the final guest pass. |
| Billfold — Gary | Guest game/prop record sections and navigation inspected. | All chart/filter/error combinations and physical touch behavior. |
| Billfold — You | Guest entry and qualification/account prompts inspected; underlying manual/verified flows pass isolated SQL and native/web fixtures. | Authorized disposable-account create/edit/settle/reopen/void/export/delete and cross-account acceptance remain **unverified**. |
| Billfold — Board / public profile | Guest board, rules, repeated sort taps, NFL qualification and empty state inspected. Decorative checkmark no longer falsely announces an unselected Most Wins sort. | Authenticated progress/rank, public opt-in/out, report/block persistence and pagination through populated real accounts. |
| Profile / Settings / help | Guest Profile, Settings close, How Gary Works, What's New **2.25**, sign-in/sign-up cancellation and modal entry/dismissal inspected. | Actual profile preference save/reopen, provider completion, consent delivery and account-cache changes on device. |
| Authentication / recovery | Guest entry/cancellation UI and executable callback/lifecycle cases pass; iOS 16 compatibility is compiled. | Physical email/Apple/Google sign-in, recovery-email return and password update are **unverified**. |
| Share / push return | Typed destination, exact-date/game and registration lifecycle fixtures pass and ship in compiled 909. | Physical APNs/FCM registration/delivery, cold/background tap and full share-card export acceptance are **unverified**. |
| Website public journey | Final 452-test suite, types, real SSR smoke and prior browser fixture interactions pass; `24b99c5c` is live READY. | Authenticated live customer/provider mutations, failure scenarios not covered by fixtures and physical mobile browser acceptance remain distinct. |

No Gary app crash was identified in the bounded simulator runtime review. A simulator accessibility duplicate-class warning is not an app crash. This is bounded evidence, not a claim of crash-free operation on every device.

Detailed page coverage: `/Users/adam.preda/Documents/ChatGPT/Gary/launch-readiness/page-completeness-review-909.md`. UI evidence: `launch-readiness/909-ui-evidence/` and `909-expanded-card-ui-review.md`. The latter explicitly records the ordinary-coordinate/AX automation distinction and the unverified full-scroll/VoiceOver boundary.

## Privacy, offer and external service state

- **Simulator privacy inventory is verified:** all **21** bundled manifests parse, declare no tracking and contain no tracking domains. Corrected simulator inventory confirms System Boot Time reason **`35F9.1`** for elapsed reading timers and **Customer Support / linked / App Functionality** for authenticated moderation reports. The corrected union is 14 categories. Source and corrected simulator manifest match. **Final signed archive independently verified at 05:39:47 UTC:** all 21 manifests match the corrected simulator inventory, the root manifest matches canonical source, and the union remains 14 categories with no tracking. App identity is `ai.betwithgary.app`, 2.25 (909), iphoneos, minimum iOS 16.0; `codesign --verify --deep --strict --verbose=2` exited 0. See `909-signed-archive-privacy-receipt.md` and its per-bundle JSON. No archive-inventory upload blocker remains.
- **Current website disclosures are deployed** in `24b99c5c`, including the reading consent description and report/support rules. Earlier inventory notes saying the prepared reading paragraph was not yet live are superseded by this release. App Store Connect must still match the actual candidate, including the added Customer Support classification and review notes/account access.
- **October offer and quoted plan prices agree** in source/public pages. Read-only inspection of the live default Stripe portal confirms cancellation, payment-method/customer updates and invoices enabled; subscription updates, price/quantity changes and update products disabled. No customer portal session, customer data request or live charge was used for QA. Physical storefront and billing flows remain unverified.
- **Supabase security advisor:** **0 ERROR / 63 WARN / 46 INFO**. Warnings include intentional public read RPCs, private service tables without client policies, existing mutable search-path functions and Auth configuration. This is not a claim that every warning is irrelevant or security is risk-free.
- **Supabase Auth follow-ups:** email is enabled; OTP expiry remains greater than one hour and leaked-password protection is off. The Pro plan supports that protection. Adam has already been asked to sign into the Supabase dashboard in Chrome; no setting change is claimed. Do not silently mark either issue resolved.
- **Apple status:** App Store Connect session expired and Adam has already been asked to sign back in. 908 upload success is established; its processing/internal inclusion is not. 909 upload succeeded at **05:42:00.321 UTC** and Apple reports processing. 910 also uploaded successfully at **05:54:41.786 UTC** and Apple reports processing. Completion, internal-group inclusion, current build selection, privacy metadata and submission state remain pending root verification.
- **Moderation/support operations:** actual staffing/coverage was requested from Adam and no reply has been recorded. Report/block interfaces, private queues and runbooks exist, but they cannot establish timely human handling or appeals coverage by themselves.

## Remaining signoff gates

| Gate | Current state / next required receipt |
| --- | --- |
| Signed 909 artifact inventory and upload | **Complete:** identity, 21 manifests / 14 categories, required API/support declarations and strict/deep signature verified; exact archive uploaded successfully. Processing is a separate gate. |
| Apple processing and selection | Upload complete. Root must verify processing completion, intended TestFlight/App Store build selection and metadata/review access after the existing sign-in request is satisfied. |
| Physical auth and recovery | **Unverified:** supported provider success/cancel/failure, email confirmation and recovery link through password update on an authorized test account. |
| Physical notification lifecycle | **Unverified:** OS permission, APNs/FCM registration and delivery, account switch/unlink, cold/background/foreground destination behavior. Fixtures do not prove provider delivery. |
| Storefront / account / billing | **Unverified:** intended storefront access/terms, account-owned portal return, authorized disposable Book/profile CRUD and account deletion. Do not make real customer charges or destructive customer-data changes for QA. |
| Reliable long-reading/accessibility input | **Partially verified:** ordinary coordinate expansion aligned. Full vertical reading, collapse and VoiceOver need reliable device/input evidence; current tool behavior does not settle them. |
| Supabase Auth settings | Pending previously requested dashboard sign-in and authorized configuration/verification of OTP lifetime and leaked-password protection. |
| Actual moderation/support coverage | Pending Adam's response; record responsible owner, coverage and support/appeal handling before claiming operational readiness. |
| Confirmed Hub label ambiguity / candidate 910 | **P2 presentation issue confirmed:** the ATL @ PHI streak context and Schwarber VS ASTROS popup refer to different date contexts; the latter is the next opponent. Pick identity, original rationale and grading are unaffected. The narrow repair labels the Hub section **Team streaks** and the opponent **NEXT GAME**. Root committed and pushed `e88b70ce` / 910; five existing optimized tests, simulator compile, signed archive and privacy/signature inventory pass. Upload succeeded at **05:54:41.786 UTC**. The actual changed-label recheck remains pending Mac unlock. Preserve the successful 909 baseline. |
| Final production truth and release receipt | **Complete at 05:54–05:55 UTC:** canonical workers, intended Astra/Sol models, all 20 edge timestamp checks pass, active 6 AM MLB slate 11/11 / zero started missing. Script exit 1 is only the preserved private plist exception. No unpushed code or deployment drift was found. |

Two lower-priority product differences remain documented rather than mislabeled as unfinished core features: native private profile preferences still require a valid handle even with public visibility off, and native/web bounded history differs for future manual entries. See `features-audit.md` and `page-completeness-review-909.md`. Neither authorizes fake leaderboard data or changing immutable published tickets.

## Verification discipline and evidence

Use isolated fixtures for integrity, privacy, leaderboard and receipt mutations. Preserve published ticket/rationale identity, pinned prediction behavior and original historical evidence. Re-run the checks invalidated by an actual change; superseded exploratory failures are not current release failures once their corrected integrated run is recorded above.

Root owns Apple processing/selection, remaining acceptance, production parity and signoff. The signed archive inventory and upload receipts are complete above. Stage only explicit owned paths; keep private configuration/credentials out of commits. A deployed edge change requires verified deployed behavior, and a native source change requires the intended built artifact. Apple approval remains an external decision.

Detailed receipts live in `/Users/adam.preda/Documents/ChatGPT/Gary/launch-readiness/`, including integrity/history, grading/cache/social authorization, profile safety, Book/leaderboard, Home ownership, Winners date/empty state, exact-game identity, push registration/routing, reading/privacy, page completeness, billing and submission audits. Older notes describing completed 909 repairs as absent or still being implemented are superseded by this ledger and their final verification receipts.

**Signoff remains NOT YET READY until the open gates above have explicit evidence or an explicit release decision from the owner.** 910 build/archive/privacy/upload and final production-truth receipts are complete. Root will append changed-label UI, physical/account, Auth, staffing and Apple processing/selection evidence when available.

## Remaining compiler warnings assessed

910 builds retain two Home local-function actor annotation warnings, two unused-variable warnings and the expected no-AppIntents metadata notice. The Home function is called from the inherited MainActor task, its timeout operation and an explicitly MainActor child task. An optimized Swift 5 probe using the actual timeout helper reproduced the warnings while actor preconditions passed through 100 suspension/resumption pairs, nested calls and defer with actor data-race checks enabled. No off-main execution was found in those paths. This is bounded annotation-debt evidence, not a declaration that every concurrency path is proved safe; no warning was suppressed and no additional source change was made.
