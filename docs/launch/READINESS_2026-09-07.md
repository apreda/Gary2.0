# Gary launch readiness — current release ledger

**Historical-game source repair verified; final app verification underway:** `41793ede` binds scouting, player details and lineup popups to the selected date and exact game. It fixes the observed September 7 NYM/MIA page borrowing September 8 context. The original pick and dated board were correct; no production data repair was needed. Final serial checks pass 63 cases across seven suites, with independent review and replay. The release owner has frozen 129 public build/test inputs at that commit and is building the combined 915 artifact. Corrected installed UI, signed archive and upload remain pending.

**September 8, 16:08 UTC: NOT YET READY for launch signoff.** Both real disposable-account deletion paths and all 30 checked residual-data relations pass; the reviewer remains intact. Current web `a729d9c1` is live and verified. Candidate 915 now combines the account guards, corrected Winners spacing and the explicitly requested profile icon/Billfold selector and historical context repair `41793ede`. The final combined archive/screenshots/upload and external acceptance gates remain open; 915 has not been uploaded and the saved App Store draft still selects 914.

Read the [current continuation and gate table](CONTINUATION_2026-09-08.md) for the authoritative latest state, the deployed MLB policy and exact test/visual limitations. The dated ledger below preserves historical evidence rather than replacing the current checkpoint.

## Historical 13:53 UTC release checkpoint

The following records the earlier 914 state and dated baselines. Its current/pending labels are superseded by the current continuation linked above.

## Release decision

**NOT YET READY for launch signoff.** Native **2.25 (914)**, source `a82c2ea6`, is signed/uploaded at **13:33:26 UTC** and independently verified **Complete / internal Beta Testing**. Actual final simulator checks show NFL **30–29–0 / −1u** and correct tomorrow context. Current READY website source **`210b2f38`** includes prior owner-bound account repairs and the deployed account-deletion migration; its latest full sweep passes **611 tests / 68 files, PostgreSQL 17.10 enabled, TypeScript and full ESLint**. Disposable-account QA is underway. Mac/App Store access and physical developer services work; in-place 913 install passed, but physical launch was denied because the phone was locked. App Store **2.25 Waiting for Review still selects 901**. Privacy labels are published with 14 types including Customer Support; subtitle, promotional text and support URL are saved. The supplied review phone/email are saved and independently visually confirmed; Save is disabled. Intended review selection/access, provider-rights evidence, physical/provider flows, Supabase Auth settings and actual moderation/support coverage remain open.

A successful source test, guest simulator check, production deployment and physical-device check establish different facts. The table below records each separately. Nothing here claims App Store approval or that every possible app defect has been eliminated.

### Completed post-911 findings

- **Football settlement, `40e93140`:** missing NFL measurements no longer become zero; NFL/NCAAF pages must complete before their game can grade; stored player IDs and ambiguous names are handled strictly; stats-row absence no longer becomes a DNP/push. Supported missing NFL TD, longest-completion and interception measurements can use exact-game play evidence, with complete scoring/passer reconciliation and bounded fetch/cancellation. All four independent-review findings were repaired. Final focused integration: 301 tests / eight files; independent reviewer: 143 tests / three files. [Implementation, observed provider evidence and explicit limits](FOOTBALL_SETTLEMENT_2026-09-08.md). Unverified play shapes and absent authoritative DNP evidence remain pending; no model guesses or manual production settlement were used for QA.
- **Native historical score labels, `7745aada`:** verified numeric scores and proven non-NFL source ordering retain labels; ambiguous archived NFL score strings remain unlabeled rather than receiving false team assignments. Home winner headlines require oriented evidence. All 79 unique focused score/compatibility tests pass, as do the optimized simulator build, signed archive and strict/deep signature. All 21 manifests / 14 categories match signed 911. **912 uploaded successfully; processing and existing-tester availability are now verified.** App Review selection and visible acceptance remain separate.
- **NFL history:** all 93 stored NFL game grades reproduce: 34 current-season preseason rows match retained original tickets and saved scores; 46 older rows reproduce from saved numeric evidence; 13 sparse playoff rows reproduce against official NFL results. Only the 34 have recoverable matching original tickets. Preseason exclusion is the public-record contract. A later 913 sport-selection path violated it; committed repair `875fc9bb` restores consistent exclusion, with actual final 914 sport-selected 30–29–0 / −1u now verified. Of 243 legacy player-attributed prop results, 231 match unique retained originals and reproduce against saved actuals; this is not an independent provider-stat check for all 231.
- **One historical measurement corrected, `c94a87d3`:** the December 21, 2025 Etienne anytime-TD win was correct but actual was saved as zero. Official scoring establishes one touchdown. Applied guarded migration `20260908065134` changes only derived actual to 1 plus the normal update timestamp, with independent production readback and 11 passing PostgreSQL cases. Original ticket/identity and win grade are preserved; [correction evidence](corrections/2026-09-08-etienne-actual.md). This supersedes older blanket statements that no derived history field changed during the goal.

The final full run is `/Volumes/KINGSTON/gary-912-backend-final.log`, 03:13:49 ET, 42.69 seconds. The preceding run passed 2,911 tests but could not initialize the 11-case correction suite because macOS AppleDouble metadata matched its migration suffix. Discovery now requires a real fourteen-digit migration filename; SQL and behavior assertions were unchanged. The final full rerun passes every suite. All 88 tested snapshot paths matched canonical source at verification; `launch-readiness/candidate-912-test-source.json` records hashes.

The earlier Mac-lock/DDI-unavailable diagnosis is superseded by the **13:53 UTC continuation**: simulator access is available, physical developer services work and build 913 installed. Phone unlock/input and actual provider/account acceptance remain required. The Billfold aggregate defect is repaired and the corrected final 914 record is visually verified; broader physical/provider acceptance remains separate. Source, install and guest evidence do not establish the remaining flows.

Canonical checkout: `/Users/adam.preda/Gary2.0`, `main`. Preserve the private local Firebase plist; do not read/edit/stage it or read SecretsLocal.swift. Historical source: 911 `04635425`, 912 score repair `7745aada`, football `40e93140`, correction `c94a87d3`. Current continuation: 913 Billfold `9cb29134`, web account ownership `2debee7b`, shared design `12f4df5a`, Billfold countability `875fc9bb`, final 914 `a82c2ea6` and current web `210b2f38`; each is committed/pushed. nflverse credit `779ab336` is locally committed with push/deployment pending. The older `candidate-912-test-source.json` remains a 912 snapshot, not a claim to cover subsequent changes.

## Release artifacts and verification

Rows marked prior/baseline preserve the status at their original verification; the current rows and continuation receipt supersede their old access limitations.

| Artifact / check | Current verified state | Evidence / remaining boundary |
| --- | --- | --- |
| Prior website baseline | **Committed, pushed and verified live before the 911 follow-up:** `24b99c5c`, Vercel deployment `dpl_8jCMDFZw3aG6pkGcEb6x4orA8RQb`, status READY. Includes exact prop tracking, honest empty/error states and current launch/reading disclosures. | Full web run: **452 tests / 63 files**, types and credential-free real-app SSR smoke pass. Logs: `/Volumes/KINGSTON/gary-909-web-final.log`, `gary-909-web-types.log`, `gary-909-web-final-smoke.log`. Authenticated provider/customer flows are separate acceptance gates. |
| Website 911 prior deployment | **Committed, pushed and READY:** `046354255d0ce70e1db9477fc0f2b8653a208415`, deployment `dpl_Cxkfi88cCyriD8dyeSRxXY9orXL6`, production deployment URL `gary20-n3td2g0ao-adam-predas-projects.vercel.app`. Includes private preferences, saved-identity/receipt handling, bounded Eastern history and independent all-date open slips. | **484 tests / 64 files pass** in `/Volumes/KINGSTON/gary-911-web-final.log`; full route generation/TypeScript, fixture SSR smoke (`gary-911-web-smoke.log`) and final targeted lint (`gary-911-web-lint.log`) all exit 0. Canonical Gary domain aliases are verified on this deployment; guest GETs of /you, /account, /privacy, /terms and /leaderboard return 200. See `launch-readiness/911-web-production-readback.json`; authenticated persistence remains separately unverified. |
| Website Sep 8 current deployment | **READY source `210b2f38`**, including `3943c69d` and prior owner-bound profile/Book/manual-save repair `2debee7b`. Migration `20260908132857` is deployed. | **611 tests / 68 files, PostgreSQL 17.10 enabled, TypeScript and full ESLint pass** in the latest web sweep. Earlier 525/66 and 504/64 runs remain dated baselines. Authorized disposable-account QA is in progress; final receipt pending. [Continuation](CONTINUATION_2026-09-08.md). |
| Native 912 verified prior candidate | **`7745aada`, 2.25 (912), committed and pushed.** Optimized simulator build and signed archive pass; no app-source warning remained. | Archive `/Volumes/KINGSTON/Gary-2.25-912-Launch.xcarchive`; strict/deep signature and 21 manifests / 14 categories verified at 07:09:41 UTC. Upload succeeded 07:11:57.117 UTC. Processing and existing-tester availability verified. Superseded by uploaded 913; historical score/source receipts remain valid within their scope. |
| Native 913 prior uploaded build | **`9cb29134`, 2.25 (913)** uploaded 12:18:40.448 UTC; independently verified Complete/internal Beta Testing at 13:01:15 UTC. | Guest Profile/Settings/auth entry/What's New/NFL Billfold/Board checked; consent toggles off. Development installation on the phone passed, but launch was denied deviceLocked. The newer uploaded artifact is 914; neither upload proves a physical/TestFlight journey. |
| Native 914 final uploaded candidate | **`a82c2ea6`, 2.25 (914)**. Final archive `/Volumes/KINGSTON/Gary-2.25-914-Hub-Brand-final.xcarchive`; uploaded **13:33:26 UTC**, UUID `7df2c9e4-fbc3-4276-962c-f22bf97d9b3a`. Release owner independently verified **Complete / internal Beta Testing**. | Actual final NFL record **30–29–0 / −1u** and tomorrow context verified. Executed Swift fixture gives −1.039919 units from 59 countable rows, preserving all 93. Pre-repair archive was held and is not this final artifact. Physical/TestFlight installation, intended review selection and approval remain separate. |
| Backend 912 full regression baseline | **295 files / 2,922 tests pass**, PostgreSQL 17 enabled, no skipped tests; football `40e93140`. | Dated `/Volumes/KINGSTON/gary-912-backend-final.log` and `candidate-912-test-source.json`. The focused 301/143 results are included, not extra totals. Later account-deletion/shared Hub changes need their own receipts; this row is not a new full-suite claim. |
| Native 909 verified baseline | **Committed and pushed:** `7e7c163a`, version **2.25 (909)**. Final optimized simulator build succeeded; **signed archive succeeded** at `/Volumes/KINGSTON/Gary-2.25-909-Launch.xcarchive`. | Signed archive inventory and strict/deep code signature verification passed. **Upload succeeded at 2026-09-08 05:42:00.321 UTC / 01:42 ET**, exit 0 / EXPORT SUCCEEDED; `/Volumes/KINGSTON/gary-909-upload.log`. Apple reports the uploaded package is processing; processing completion, group inclusion and selected submission build are not yet verified. |
| Native 910 narrow follow-up | **Committed and pushed `e88b70ce`**: **Team streaks** and an explicit **NEXT GAME** label, with wrapping. Five existing optimized Hub tests pass. Final optimized simulator build and signed archive pass. | Archive `/Volumes/KINGSTON/Gary-2.25-910-Launch.xcarchive`; logs `gary-910-simulator-build.log`, `gary-910-archive.log`. All 21 manifests / 14 categories match signed 909, root manifest matches source, strict/deep signature passes. **Upload succeeded 05:54:41.786 UTC**, exit 0 / EXPORT SUCCEEDED; `/Volumes/KINGSTON/gary-910-upload.log`. Final changed-label visual recheck is pending the already-requested Mac unlock. |
| Native 911 prior follow-up | Full optimized simulator build **succeeded at 02:12 ET** after the private-preference, empty-history-copy and warning cleanup changes. No app-source warnings remain; the AppIntents metadata notice remains. | Focused actual native profile tests **3/3** and warning checks **9/9** pass. Signed archive **succeeded at 02:17 ET**; `/Volumes/KINGSTON/gary-911-archive.log`. At **06:19:12 UTC**, strict/deep signature and all 21 manifests / 14 categories passed; root privacy content matches canonical and all privacy declarations match signed 910. Archive: `/Volumes/KINGSTON/Gary-2.25-911-Launch.xcarchive`. **Upload succeeded at 06:21:31.199 UTC**, exit 0 / EXPORT SUCCEEDED; `/Volumes/KINGSTON/gary-911-upload.log`. Apple reports processing; completion and selection remain unverified. No new UI acceptance is claimed while the Mac remains locked. |
| Backend 911 prior regression | **291 files / 2,769 tests pass**, PostgreSQL 17 enabled, no skipped tests. | Final run `/Volumes/KINGSTON/gary-911-backend-final.log`, started 02:13:46 ET, duration 43.29s. External-disk AppleDouble metadata files are excluded; actual tests are not excluded. The earlier 289-file / 2,766-test 909 run remains historical evidence. |
| Native edge helpers | **217 tests pass**, unchanged by the 911 profile/history/warning follow-up. | Parser, routing, registration and related helper evidence remains valid for its tested boundaries. |
| Guest simulator UI | Prior 909/912/913 guest checks plus actual final 914 NFL 30–29–0 / −1u, tomorrow context and sampled Hub/Fantasy displays. Consent toggles were off in the 913 Settings check. | [Actual evidence and limits](CONTINUATION_2026-09-08.md). Corrected Billfold display is closed; guest UI does not establish physical auth, push, billing or private CRUD. |
| Previous 908 release | Version 2.25 (908) passed optimized build, signed archive and three cold launches; uploaded successfully at 2026-09-08 03:52:58 UTC. | `/Volumes/KINGSTON/Gary-2.25-908-Responsiveness-final.xcarchive`. Do not reupload. Upload-time processing/access limitations remain historical; 914 is now independently verified internal Testing. App Review still uses 901. |

The verified deployed-baseline website smoke verifies real Next rendering, result/export consistency and discovery endpoints against isolated fixture services. Browser checks additionally covered expanded fixture reasoning, the 1–1 results record, repeated leaderboard controls and honest qualification states. Neither check places real bets, creates fake standings or proves live account-provider behavior.

## Completed integrity and operational repairs

| Area | Implemented and deployed evidence | Limits of the evidence |
| --- | --- | --- |
| MLB game/prop settlement | `e4663ccf` repairs pitcher-versus-batter routing, strict count parsing, exact player/game identity, incomplete box evidence, game spread/total/push parsing and matching local/cloud behavior. 239 focused cases, isolated database contention checks and both Deno entrypoints passed. Deployed read-only checks inspected 12 game picks and 33 props / 24 computed, with no invalid identity, unavailable box or missing-stat failures. | No manual regrading or history correction was performed. Read-only dry checks do not mutate published picks or original rationale. |
| Historical reconciliation | **All 54 exposed pitcher-walk tickets** from August 8–September 7 matched final provider boxes: zero incorrect and zero unverified tickets. **All 428 game-results rows** were consistent with the new parser using stored ticket/matchup/final-score evidence. | The 428-row check is stored-evidence consistency, not an independent full provider refetch; it excludes the separate NFL results table. |
| Pregame prop persistence | Publication deadline, wall-clock enforcement after contention, immutable retry receipt comparison and exact ticket preservation are deployed. | Isolated contention/retry tests passed; production history was not rewritten. |
| Grading authorization | `1e731a4e` requires service authorization before mutable grading. Legitimate cron callers use Vault service credentials with original schedules/timeouts. 113 focused tests plus 25 actual handler/auth cases passed; deployed public mutation/preview calls return 403, service game-read-only/prop-dry calls return 200. | Public read-only social evidence remains available. No live grades were triggered for QA. |
| New-pick and personal-result push | `b2c5b791` repairs exact source identity, current access logic, durable per-device delivery receipts, concurrency/retry behavior and service-only dispatch. `17155b3c` adds personal settlement destinations and accurate five-win-streak copy. | 24 handler/database and 12 transport/source cases passed for new-pick delivery; intercepted actual FCM orchestration covered two users/three devices and 22 personal-result edge cases. No QA notification was sent. Physical delivery remains unverified. |
| Device/account registration | `02f4121a` binds personal registration to authenticated device sessions and serialized native account changes. Live migration preserved 234 tokens / 232 active tokens and cleared unproved legacy personal account links. Older clients retain generic alerts; personal results resume after a current signed-in app registers. | Twelve isolated SQL cases passed. Private installations enforce RLS with no client reads. No QA device was registered in production. |
| X mutation authorization | `c357480c` secures ten mutation endpoints and four legitimate internal callers, preserving cron cadence/timeouts. 147 focused and 77 existing social cases, all ten Deno entrypoints and 36 reconciled auth/cron/view cases passed. | All ten deployed endpoints reject anon with 403; service requests with missing required inputs reach expected validation without X calls. No QA post, reply or deletion occurred; posting strategy was unchanged. |
| Cache writer authorization | `68065a27` secures live-scores and MLB lineup refresh with legitimate callers/cron cut over to Vault. Public writer calls return 403; public cache reads remain 200. | Focused actual handler checks and both Deno entrypoints passed. Native public reading was preserved. |
| Public analytics views | Five views now enforce source RLS under the caller and expose SELECT-only client grants; all five public REST samples remained 200. | Narrow hardening; no private user/experiment leak had been demonstrated before the fix. |
| Profile safety | `4b02b98c` and subsequent native/web releases add authenticated report/block/blocked-list controls, private report storage, service-only moderation and public rules/support links. Thirteen isolated SQL cases and 43 focused web cases passed. | Technical queues and permissions do not establish actual moderation staffing or response coverage; that remains open. |

Applied migration versions are the actual Supabase versions, including reconciled local filenames:

| Applied migration | Purpose |
| --- | --- |
| `20260908031714_enforce_prop_publication_deadline.sql` | Post-lock wall-clock check and service-only immutable prop publication |
| `20260908032715_reliable_pick_push_delivery.sql` | Private durable delivery receipts and retry claims |
| `20260908032716_secure_pick_notification_cron.sql` | Vault authorization; original active five-minute cadence |
| `20260908033025_secure_grading_cron.sql` | Vault service authorization for both unchanged grading schedules/timeouts |
| `20260908034216_secure_social_service_cron.sql` | Vault authorization; cron 3 remains active every 15 minutes with 120-second timeout |
| `20260908034218_secure_public_analytics_views.sql` | Caller RLS and client SELECT-only analytics view grants |
| `20260908034829_secure_cache_refresh_cron.sql` | Vault authorization for unchanged cache schedules/timeouts |
| `20260908035634_profile_safety_controls.sql` | Private reports, block controls and authenticated/service RPC boundaries |
| `20260908041241_authenticated_push_registration.sql` | Authenticated account/device binding and protected installation capabilities |

Credentials were stored in Vault without printing them or embedding them in repository SQL. Post-cutover observations of ordinary scheduled work returned 200 for both graders; scores reported 12 games/no errors; lineups reported 15 games/15 writes; the X scheduler returned its expected outside-posting-window response. These were observations of scheduled work, not QA-triggered mutations.

The earlier production-truth receipt confirmed canonical workers, correct Astra/Sol choices and **MLB 11/11 published with zero started games missing picks**. Later cache deployment and normal cron observations closed that receipt's two then-pending function checks. Final production truth at **05:54 UTC** on committed `0638894c` confirms canonical scheduler PID 79083 and Winners PID 89023, correct Astra games / Sol props, all 20 edge timestamp checks passing and no unpushed commits. Exit 1 is solely the one preserved private GoogleService-Info.plist local exception. Its calendar-day Sep 8 query reports 0/0 because that slate is not populated yet; a separate read-only 6 AM slate query at **05:55:18 UTC** confirms the active Sep 7 board is **11/11 MLB games published, zero started without a pick**. This is timestamp deployment parity plus the earlier deployed behavior checks, not a fresh comparison of every deployed source byte. Receipt: `/Volumes/KINGSTON/gary-910-production-truth.log`.

## Completed native and web behavior

### 911 follow-up: committed, verified, deployed and uploaded

- **Private preferences are now complete in both forms.** Favorite sports and unit preferences can be saved without claiming a public handle. Existing identity survives a blank replacement handle, including an existing public profile; a brand-new public profile still requires a valid name. Nonempty draft names retain strict validation and account ownership remains enforced. A new unnamed account cannot silently lose avatar/bio edits: those fields explicitly require a profile name. No API/schema change was needed. The previous audit was incorrect that the old web form already allowed handle-free saves: its HTML `required` field blocked them too.
- **Native saved zero/unset preferences reopen correctly.** Nil/zero loads as an empty editor field; positive values round-trip without precision loss. Invalid saved or entered values remain invalid. Blank retains the native hypothetical **$100 per unit** default and sends the existing zero/unset API value. Web zero means unit display, as before. This is an explicit display convention difference, no longer a broken Save path. Web UI derives its post-save unit/identity state from the actual server receipt.
- **History and open slips have distinct, consistent date semantics.** Web 7D/30D/Season history now ends at Eastern today, matching native; All time remains unbounded. Open slips stay reachable across all dates while respecting source, search, sport, status and favorite filters. Future self-graded results no longer enter a past-period record/chart/export, and old unresolved slips no longer disappear merely because a recent history window is selected. Native empty-history wording no longer falsely says no bets match while open slips are visible.
- **Compiler warnings are repaired.** Explicit MainActor annotation and unused-variable cleanup replace the prior warning-assessment-only state. The full optimized 911 build succeeds without app-source warnings; no warnings were suppressed. The expected AppIntents metadata-generation notice is separate.
- **Evidence:** focused native profile **3/3** plus warning **9/9**, web private-form **13/13** plus history **61/61**, with focused types/lint passing. Final integrated results are **2,769 backend tests / 291 files** and **484 web tests / 64 files** in `/Volumes/KINGSTON/gary-911-backend-final.log` and `gary-911-web-final.log`. The earlier attempt had 2,768 passing cases and one outer five-second timeout while a valid Swift fixture compile took seven seconds during a concurrent SDK build. Its outer limit now matches the existing bounded 60-second compile / 10-second execution budget with 75 seconds overall; assertions were unchanged. The successful final rerun supersedes that resource-contention timeout. Full route-generation/TypeScript checks also exit 0, and the final real fixture smoke reports success in `/Volumes/KINGSTON/gary-911-web-smoke.log`. Final code is committed and pushed as `04635425`; final targeted lint exits 0. Its production deployment is READY on `www.betwithgary.ai` and the other configured Gary domains. Guest GETs of /you, /account, /privacy, /terms and /leaderboard returned 200, with the reading disclosure present. These checks do not establish authenticated live persistence.

### Previously shipped baseline behavior

- **Date and request ownership:** Home's full/rolling loads now reject obsolete date/account/request results, including cold 6 AM rollover and late enrichment. Winners anchors its header and prop grades to the accepted board date, refreshes the visible current-date board/results every 90 seconds, and checks rollover every 30 seconds without overriding manual history. Empty-slate phase handling distinguishes upcoming review, all-started final empty, unknown schedule and unavailable sources. All are integrated, compiled and covered by executable regressions.
- **Game identity and data contracts:** MLB lineups use exact game/date identity without a matchup fallback; weekly NFL aliases normalize correctly. Optimized Hub league routing preserves 200 groups / 800 correctly scoped outcomes. The starter-record/two-start-schedule Codable union restored the actual public sample from 250/264 to **264/264** decoded records while preserving complete cache equality.
- **Book and leaderboard:** Exact verified receipts, pregame lock checks, fractional/unknown kickoff handling, fun-lane eligibility, directory failure/retry, private manual CRUD/export, streaks, qualification, pagination and public privacy are implemented. Public profile repeat-window loading, first-load false-zero Book statistics and standalone personal qualification were repaired and deployed. The real empty leaderboard is valid min-five decided-call/opt-in behavior; `bookPreviewCast` remains false.
- **Web exact props:** Featured cards pass game/line/side identity; same-player doubleheader sign-in returns arm only the intended ticket; explicit confirmation remains required. Unknown time is visibly unavailable, kickoff is rechecked before writes, doubleheaders get separate correctly timed panels and same-game MLB HR remains grouped with core props. Required-feed failure no longer masquerades as an unpublished empty board. Historical Winners empty copy no longer promises today's reviews.
- **Auth, access and onboarding:** Correct first-launch Picks destination, accessible intro, selected-plan disclosures, founding wording, complete password-recovery callback flow, Settings navigation and modal close controls are integrated. Native typed push navigation, serialized authenticated registration and profile safety controls compile for the supported iOS 16 target and pass their focused execution fixtures.
- **Reading measurement:** A separate optional, off-default consent choice counts in-memory sessions and five continuously visible seconds of expanded original game/prop card reasoning on instrumented Home/Winners/Picks surfaces. Active tab, foreground, viewport and modal gates apply; it excludes collapsed previews and does not claim Hub/Fantasy/chat coverage or cross-launch retention. Payloads omit account, installation, notification, pick/player and reasoning identifiers/content. Upgrades do not broaden checkout consent. Timing, lifecycle, payload, viewport and consent cases pass; no live QA analytics events were sent.

Leaderboard, private Book, verified receipts, streaks, profiles, account access, deletion and Fantasy are real implemented features. Sportsbook account import and Talk to Gary are intentionally absent launch features, not dead controls to activate. Manual bookmaker metadata is not a FanDuel/DraftKings integration. Retired NHL/NCAAB and finished tournament content must not be reintroduced as current coverage.

## Actual 909 page and interaction acceptance

These rows are dated **909 guest UI evidence**. They distinguish observed UI from unverified authenticated/device boundaries and do not claim a new visual pass for 910, 911 or 912. Their dated input limitations are preserved here; the latest continuation has restored Mac access and later 914 checks above.

| Surface | Actual 909 guest evidence | Remaining boundary |
| --- | --- | --- |
| Fresh launch / disclaimer / intro | Final iPhone SE journey: welcome → disclaimer → Access Picks → correct **tab 3** → intro. Normal and maximum text size keep the final dock visible; iPhone 17 dock also checked. | Physical-device accessibility and full first-use journey remain separate. |
| Home / date context | Guest Home and The Daily Card inspected, with the final iPhone 17 title confirmed in saved accessibility evidence. Executed full/rolling ownership cases cover partial/failure/cancellation/account/6 AM boundaries. | Physical touch/VoiceOver and authenticated private summary; simulator source fixtures are not a real overnight production observation. |
| Winners / history / plans | Closed-slate guest board and its honest no-selection state observed. Empty-state, board-date, history-preservation and periodic-refresh helpers execute against shipping source. | Authenticated founding/paid/revoked-access UI and physical storefront behavior; October entitlement boundaries have isolated fixtures but are not observed live in October. |
| Picks / game / props | Settled game and prop cards, original reads and correct accepted-slate header observed. After midnight the rechecked header correctly displayed **TODAY SEP 7**, matching the 6 AM slate. The actual final team modal was visually checked: stored prices say **SAVED ODDS**, distinct from live score status. | Actual before/at/after kickoff account action, every sport/postponed case and physical accessibility need their own acceptance. |
| Expanded long reasoning | On the rebuilt 909 Cubs card, ordinary coordinate-tap expansion stayed aligned with full visible text width. Intrinsic vertical text sizing and removal of the expansion spring are retained. | Sky's AX-targeted click shifted the rail; its drag also failed to scroll ordinary Home. That distinguishes a tool-specific reproduction from a proved app touch/VoiceOver bug. Complete vertical reading, collapse and VoiceOver remain unverified; no speculative rail/gesture rewrite was made. |
| Hub / player / game detail | Guest Hub stories, team/player routes and expanded pitcher stats were observed. **OPP AVG** is corrected in source; the attempted final More Stats/header recheck was interrupted by the Mac locking, so that final visual check is unverified. Exact routing and optimized league/model cases pass. | Physical long-reading gestures/accessibility; final pitcher header recheck after the already-requested Mac unlock; repaired 910 next-opponent labels awaiting visual recheck below. |
| Fantasy | Implemented MLB/NFL briefing and controls remain covered by prior source/optimized fixtures. Final candidate includes honest missing-data copy. | Every interactive sport/format/filter and long-reasoning journey is not claimed fully exercised in the final guest pass. |
| Billfold — Gary | Guest game/prop record sections and navigation inspected. | All chart/filter/error combinations and physical touch behavior. |
| Billfold — You | Guest entry and qualification/account prompts inspected; underlying manual/verified flows pass isolated SQL and native/web fixtures. | Authorized disposable-account create/edit/settle/reopen/void/export/delete and cross-account acceptance remain **unverified**. |
| Billfold — Board / public profile | Guest board, rules, repeated sort taps, NFL qualification and empty state inspected. Decorative checkmark no longer falsely announces an unselected Most Wins sort. | Authenticated progress/rank, public opt-in/out, report/block persistence and pagination through populated real accounts. |
| Profile / Settings / help | Guest Profile, Settings close, How Gary Works, What's New **2.25**, sign-in/sign-up cancellation and modal entry/dismissal inspected. | Actual profile preference save/reopen, provider completion, consent delivery and account-cache changes on device. |
| Authentication / recovery | Guest entry/cancellation UI and executable callback/lifecycle cases pass; iOS 16 compatibility is compiled. | Physical email/Apple/Google sign-in, recovery-email return and password update are **unverified**. |
| Share / push return | Typed destination, exact-date/game and registration lifecycle fixtures pass and ship in compiled 909. | Physical APNs/FCM registration/delivery, cold/background tap and full share-card export acceptance are **unverified**. |
| Website public journey | Prior fixture SSR/browser acceptance remains dated evidence. Current READY web source `210b2f38` includes the owner-bound repair; latest full sweep passes 611 tests / 68 files plus types/full lint. | Authorized live disposable-account QA is underway; await its final receipt. Physical mobile/provider acceptance remains distinct. |

No Gary app crash was identified in the bounded simulator runtime review. A simulator accessibility duplicate-class warning is not an app crash. This is bounded evidence, not a claim of crash-free operation on every device.

Detailed page coverage: `/Users/adam.preda/Documents/ChatGPT/Gary/launch-readiness/page-completeness-review-909.md`. UI evidence: `launch-readiness/909-ui-evidence/` and `909-expanded-card-ui-review.md`. The latter explicitly records the ordinary-coordinate/AX automation distinction and the unverified full-scroll/VoiceOver boundary.

## Privacy, offer and external service state

- **Simulator privacy inventory is verified:** all **21** bundled manifests parse, declare no tracking and contain no tracking domains. Corrected simulator inventory confirms System Boot Time reason **`35F9.1`** for elapsed reading timers and **Customer Support / linked / App Functionality** for authenticated moderation reports. The corrected union is 14 categories. Source and corrected simulator manifest match. **Final signed archive independently verified at 05:39:47 UTC:** all 21 manifests match the corrected simulator inventory, the root manifest matches canonical source, and the union remains 14 categories with no tracking. App identity is `ai.betwithgary.app`, 2.25 (909), iphoneos, minimum iOS 16.0; `codesign --verify --deep --strict --verbose=2` exited 0. See `909-signed-archive-privacy-receipt.md` and its per-bundle JSON. No archive-inventory upload blocker remains.
- **Current website disclosures remain deployed** with READY `210b2f38` (preserving prior reading/report/support disclosures). nflverse credits `779ab336` are locally committed, pending push/deployment. App Store labels were published around 13:07 UTC with all 14 categories, including Customer Support / App Functionality / linked to identity and no tracking selected. Final candidate processing is verified; review notes/access, screenshots, rights and selection remain separate.
- **October offer and quoted plan prices agree** in source/public pages. Read-only inspection of the live default Stripe portal confirms cancellation, payment-method/customer updates and invoices enabled; subscription updates, price/quantity changes and update products disabled. No customer portal session, customer data request or live charge was used for QA. Physical storefront and billing flows remain unverified.
- **Supabase security advisor:** **0 ERROR / 63 WARN / 46 INFO**. Warnings include intentional public read RPCs, private service tables without client policies, existing mutable search-path functions and Auth configuration. This is not a claim that every warning is irrelevant or security is risk-free.
- **Supabase Auth follow-ups:** email is enabled; OTP expiry remains greater than one hour and leaked-password protection is off. The Pro plan supports that protection. Adam has already been asked to sign into the Supabase dashboard in Chrome; no setting change is claimed. Do not silently mark either issue resolved.
- **Current physical-device readiness, September 8 continuation:** paired iPhone 17 Pro developer services/DDI now work; root's in-place 913 update install succeeded. iOS denied its launch because the phone was locked (`deviceLocked`), not because of a demonstrated Gary crash. Mirroring Unable to Connect is separate. Mac/simulator access has returned; physical app/provider/account/gesture acceptance remains unverified. [Sanitized evidence](CONTINUATION_2026-09-08.md).
- **Apple status, fresh authenticated readback:** final 914 is Complete/internal Beta Testing; the live 2.25 review form remains Waiting for Review with selected build 901. Subtitle, promotional text and support URL are saved. The supplied review phone/email are saved and independently visually confirmed; Save is disabled. Working reviewer credentials, final screenshots/notes, intended selection and approval remain open. The 14-type privacy labels were published around 13:07 UTC, including linked Customer Support/App Functionality. Earlier upload-time limitations remain historical.
- **Moderation/support operations:** actual staffing/coverage was requested from Adam and no reply has been recorded. Report/block interfaces, private queues and runbooks exist, but they cannot establish timely human handling or appeals coverage by themselves.

## Remaining signoff gates

| Gate | Current state / next required receipt |
| --- | --- |
| Current candidate and tested source | **914 artifact and sampled corrected UI delivered:** `a82c2ea6`, signed/uploaded/Complete/internal Testing, actual NFL 30–29–0 / −1u and tomorrow context verified. READY web `210b2f38`, 611 tests / 68 files, PostgreSQL 17.10, types/full lint. Root still owns disposable-account QA completion, shared Hub corrections, attribution deployment and final runtime readback. Backend 2,922/295 is the dated 912 baseline. |
| Apple review selection and metadata | **Still open:** live 2.25 Waiting for Review selects 901. 914 internal Testing, 14-type privacy publication and saved subtitle/promotional text/support URL are verified. Supplied review phone/email saved and independently visually confirmed; Save disabled. Complete working reviewer access, final materials and intended verified build selection. No approval or new selection claimed. |
| Provider content rights | Public commercial grants verified for BDL/The Odds API; nflverse attribution `779ab336` implemented but pending deployment. Document the MLB StatsAPI/NFL direct-feed bases and retrieve existing Tank01 terms; reconcile Apple Content Rights using evidence. [Audit](PROVIDER_RIGHTS_2026-09-08.md). |
| Physical device and input | **Mac/simulator and developer services available; 913 install succeeded.** iOS launch denied deviceLocked; unlock the phone for actual flows. Mirroring remains Unable to Connect separately. Pairing/install do not establish physical acceptance. |
| Physical auth and recovery | **Unverified:** supported provider success/cancel/failure, email confirmation and recovery link through password update on an authorized test account. |
| Physical notification lifecycle | **Unverified:** OS permission, APNs/FCM registration and delivery, account switch/unlink, cold/background/foreground destination behavior. Fixtures do not prove provider delivery. |
| Storefront / account / billing | Authorized disposable web-account QA is underway after account-deletion deployment. Final live receipt pending. Physical storefront access, account-owned portal return and billing remain separate; do not use real customer charges or destructive customer-data changes for QA. |
| Reliable long-reading/accessibility input | **Partially verified:** ordinary coordinate expansion aligned. Full vertical reading, collapse and VoiceOver need reliable device/input evidence; current tool behavior does not settle them. |
| Supabase Auth settings | Pending previously requested dashboard sign-in and authorized configuration/verification of OTP lifetime and leaked-password protection. |
| Actual moderation/support coverage | Pending Adam's response; record responsible owner, coverage and support/appeal handling before claiming operational readiness. |
| Hub label and date context | Prior 910 Team streaks/NEXT GAME and 912 OPP AVG checks retain their scope. Actual final 914 sampled Hub/Fantasy, off-slate and tomorrow context checks are now recorded in the continuation. This is not every team/date/format or physical interaction. |
| Final production truth and release receipt | Root must finish a readback after current shared changes. The 07:18 UTC 912 and 10:01 heartbeat production receipts remain historical and are not evidence for later account-deletion/Hub source. No production grading or customer notification should be triggered as QA. |

The two previously documented preference/history differences were confirmed as useful-flow defects and are **resolved in 911 source and focused/integrated tests** above. The older remaining-P3 descriptions in `features-audit.md` and `page-completeness-review-909.md` are superseded on those points. Release/device acceptance remains separate; no fake leaderboard data or immutable ticket changes were introduced.

## Verification discipline and evidence

Use isolated fixtures for integrity, privacy, leaderboard and receipt mutations. Preserve published ticket/rationale identity, pinned prediction behavior and original historical evidence. Re-run the checks invalidated by an actual change; superseded exploratory failures are not current release failures once their corrected integrated run is recorded above.

Root owns remaining account/physical/provider acceptance, final runtime/deployment readback, intended Apple review selection/access and rights, Auth configuration, support coverage and signoff. The final 914 archive/upload and sampled corrected UI are delivered. Completed earlier artifacts remain preserved; stage only explicit owned paths and preserve private configuration. Apple approval remains an external decision.

Detailed receipts live in `/Users/adam.preda/Documents/ChatGPT/Gary/launch-readiness/`, including integrity/history, grading/cache/social authorization, profile safety, Book/leaderboard, Home ownership, Winners date/empty state, exact-game identity, push registration/routing, reading/privacy, page completeness, billing and submission audits. Older notes describing completed 909 repairs as absent or still being implemented are superseded by this ledger and their final verification receipts.

**Signoff remains NOT YET READY until the open gates above have explicit evidence or an explicit release decision from the owner.** The current website is live, final 914 is internal Testing, corrected Billfold UI is verified and privacy labels are published. Final disposable-account QA, physical/provider acceptance, rights, Auth settings, staffing and intended App Review selection/access remain open.

The following dated production/access records are preserved as history. Their Mac-lock, DDI, App Store login and current-candidate statements are superseded by the 13:53 UTC checkpoint above.

## Historical final 911 production receipt

At **06:20 UTC**, production truth on `5039edb4` confirms canonical scheduler PID 79083 and Winners PID 89023; intended Astra game and Sol prop models; all 20 edge timestamp checks passing; and no unpushed commits. Exit 1 is solely the one preserved private Firebase plist difference. The calendar-day Sep 8 slate is not populated; the separate active 6 AM slate read at **06:21:30 UTC** confirms Sep 7 MLB **11/11 published with zero started without a pick**. Logs: `/Volumes/KINGSTON/gary-911-production-truth.log`; `launch-readiness/911-final-release-receipt.json`. Later changes only record documentation receipts. No runtime or provider configuration changed after this check.


## Historical final 912 production receipt

At **07:18 UTC**, production truth on `792e35ca` confirms canonical scheduler PID79083 and Winners PID89023, intended Astra game/Sol prop models, all20 edge timestamp checks passing and no unpushed commits. Exit1 is solely the preserved private Firebase plist difference. Calendar-day Sep8 is not yet populated; separate active 6AM-slate SQL at **07:18:44 UTC** confirms Sep7 MLB **11/11 published, zero started without a pick**. The Vercel deployment `dpl_Cxkfi88cCyriD8dyeSRxXY9orXL6` is freshly confirmed READY and aliased to Gary’s canonical domains; `git diff 04635425 HEAD -- web` is empty. Logs: `/Volumes/KINGSTON/gary-912-production-truth.log`; detailed sanitized receipt: `launch-readiness/912-final-release-receipt.json`.

The fresh approved simulator-access recheck still returns **Mac locked; automatic unlock unavailable**. No UI action followed that failure. Existing Apple/Supabase login and support-coverage requests have no recorded reply. The same external prerequisites have persisted across the original goal turn and both automatic continuations. The newly found source defects are now repaired, independently reviewed, tested and committed; 912 is signed/uploaded. Required remaining acceptance cannot progress without restored access or owner input. Launch signoff remains **NOT YET READY**; do not claim the goal complete or fill these gates with fixture evidence.

Later documentation-only receipt commits do not change the tested or deployed runtime source. Resume with the open acceptance gates and existing preserved artifacts when access/input returns; do not recreate or reupload completed releases.


## Historical 10:01 UTC heartbeat — Apple and launch execution

Official Apple emails independently close 912 processing and existing-tester availability, as recorded above. The earlier “processing unverified” entries are dated upload-time evidence, superseded for 912 by this receipt. App Review selection/approval and actual device acceptance remain open; the fresh approved UI read still reports a locked Mac. Xcode Cloud’s separate redacted Firebase failure remains its previously documented configuration dependency, without affecting the signed local 912 upload.

The current six-workstream marketing review is [September 8 execution review](../../GaryMarketing/launch-2026-09/EXECUTION_REVIEW_2026-09-08.md). It records full-day 12/12 pregame publishing, the deployed narrow X excerpt guard, current consented cohort/Book evidence, attribution repair and updated video-delivery manifests. These marketing changes do not reopen or replace the verified 912 native artifact. Public launch signoff still requires the remaining actual account/device/operations gates.

## Resumed 912 simulator acceptance — 11:58 UTC

Mac access returned. The simulator was still running 909; the existing optimized 912 product was installed and its version/executable verified. Fresh actual UI evidence confirms NFL next-slate rendering with expanding/collapsing schedule detail, restored MLB starter-record explanation/team routing, the corrected pitcher `OPP AVG` label with More Stats/Less, and visible `NEXT GAME` labels in Streak watch. [Screenshots, source reconciliation and limitations](../../audit-evidence/resumed-912-2026-09-08/README.md).

This supersedes the blanket Mac-lock blocker for this dated pass and closes the sampled pitcher-label visual check. It does not establish private-preference saves, historical NFL score acceptance, every Team streaks surface, physical auth/push/billing/account flows, or VoiceOver/long-reading acceptance. Scroll/drag attempts produced no visible movement through the test controls. Chrome's existing Apple tab still pointed to failed sign-in, and fresh page reads timed out; review selection and tester-group membership remain unverified. No source change, rebuild, archive, upload or external write occurred. Launch signoff remains NOT YET READY.

Post-heartbeat production readback on `8411083c` verifies canonical workers, intended models, all 20 edge timestamp checks including social-auto-post v108, and no unpushed commits. Sep 8 MLB has 15 future games and zero started without a pick. Exit 1 is solely the preserved private plist. [Sanitized final receipt](../../GaryMarketing/launch-2026-09/evidence/production-truth-2026-09-08.json). The website attribution deployment and public served-code check are verified separately in the current execution review. Subsequent receipt-only commits preserve runtime source.
