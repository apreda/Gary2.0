import Foundation

// MARK: - SecretsLocal (template)
//
// Copy this file to `SecretsLocal.swift` and fill in your real keys.
// `SecretsLocal.swift` is gitignored so the actual values never end up in git.
//
//   cp ios/GaryApp/SecretsLocal.swift.example ios/GaryApp/SecretsLocal.swift
//   # then edit SecretsLocal.swift with your real keys

enum SecretsLocal {
    /// xAI Grok API key — used for TTS playback in GaryVoiceService.
    /// Get a key at https://console.x.ai/
    static let xaiAPIKey: String = "REPLACE_WITH_YOUR_XAI_API_KEY"
}
