# Content-stages alert and the Codex cap — September 17, 2026

Adam reported the 8:05 AM alert "Coverage failure: content-stages" and asked why it failed, whether anything else had, and then to make the system work with clean code. Times ET.

## What actually failed

1. **The alert (8:00 AM run).** Only the `wire` stage failed, for NCAAF. `codex-plus` was still answering then (the Wire's MLB and NFL grounded searches ran on it), but the NCAAF Codex search hung for its full 60-second window. The Wire spends one 150-second clock across four leagues and gave each league's native web-search fallback only what was left after the Codex search timed out, about ten seconds, so the fallback was cancelled at "Wire grounded call deadline exceeded" and NBA was deferred. The 11:00 AM run failed the same way. `board`, `insights`, `cards`, `results`, `recaps` were all fine in `host-health-latest.json`.
2. **Codex usage cap.** `codex-plus` reached its five-hour usage limit yesterday 7:30–9:22 PM and again today from about 11:00 AM until 1:01 PM (the 8:00 AM content run plus two MLB props runs filled the window). The personal profile is reserved for final game-pick recovery, so every Sol call fails during a cap. Hub lane prose falls back to computed details, props fall back to Claude, game picks (Fable + Sonnet) and X posts (Sonnet) are unaffected.
3. **Winners prop selection during the cap:** its Claude CLI fallback died with `spawn claude ENOENT`. The Winners launchd job never carried `CLAUDE_CLI_PATH` or `~/.local/bin` on PATH (the scheduler plist does). Once the fallback could spawn (run 3, 11:20 AM) it still failed: the first batch read took 325 s on Sonnet, the second was cut by the 8-minute deadline, and the run's budget was exhausted before the global rank. The reads were sequential; the budget is sized for Sol.
4. **Winners MLB curation:** failed twice at 11:05 and 11:06 with "One complete original record exceeds the reading budget; it was not truncated." Since the Sep 16 bullpen/receipts repair (`87ecd166`), June-engine games retain every tool response (today's records: 0.8–1.3M characters, tool transcripts up to 636K; yesterday's: 60–77K with empty transcripts). The comparative curation packet inlined those transcripts, so one candidate exceeded the reader's context and the whole MLB curation failed. Coverage still admits by schedule at T-60 (`ensure_winners_window_coverage`), so the board is filled, but not by Gary's comparative read.
5. **Not a failure, for the record:** the Winners worker was not stalled. The live `claim_winners_candidate` (migration `20260917011158_winners_daily_props.sql`) deliberately retired the checklist-review queue for current dates; games are admitted by `runDailyCuration`, props by `runPropsSelection`. The worker polls, finds nothing to claim, and logs nothing — which is why it looked silent.

## Changes (all on `main`, explicit pathspecs, tests added)

| Commit | Change |
|---|---|
| `63b0ca86` | `src/services/insights/wireBudget.js` (new): each Wire league window keeps a reserved native-search share (`WIRE_FALLBACK_RESERVE_MS` 40 s); `run-wire-items.js` sizes windows through it; `callWireModel` goes straight to the native search when no subscription share is left. Tests: `wireBudget.test.js`, `wireModel.test.js`. |
| `b8200365` | `scripts/launchd/com.gary.winners.plist` (+ installed plist): `CLAUDE_CLI_PATH=/Users/adam.preda/.local/bin/claude` and `~/.local/bin` on PATH, same as the scheduler. Worker reloaded (bootout/bootstrap), PID 74183 then 76650. |
| `4ddc5c8e` | `winnersProps.js`: prop selection reads its complete 500 KB batches two at a time (`READS_IN_FLIGHT`, the daily curation's shape) instead of one after another with the whole remaining window per read. Under the cap the Sonnet CLI read takes ~325 s a batch, so a two-batch slate (today's run 3: 4 candidates) could never finish inside the 8-minute run; side by side it fits, then the global rank follows. Batch order preserved, nothing truncated. Test: three single-candidate batches → two in flight, then one, then the rank. |
| `760e807f` | `winnersProps.js`: `readPropSelection` is the one reading of the selection contract and returns `{value, reason}`; `parsePropSelection` is its value. A rejected batch reading now fails the run with the rule and row it broke (e.g. `row 2: source_quote is not an exact passage of the original source_record`) and the complete rejected reading is written to `winners-stdout.log`. Run 4 (11:41 AM, two Sonnet batches side by side, 413 s) had failed with the bare "Incomplete or unsupported prop comparison" and nothing to diagnose. |
| `0cf12bb1` | `originalGameEvidence.js`: `curationSourceDesk()` = desk text + research briefing + recorded MLB judgment, whole; `reviewSourceDesk()` = that plus every exact tool response (factual review). `winnersCuration.curationPacket` reads the curation record. Test: transcripts stay out of the comparative packet and a candidate with a 700K transcript batches within budget. Nothing is truncated anywhere. |

Verification: Wire suites 16/16; Winners suites 101/101; `productionTruth.test.js` 18/18; full backend suite 400 files / 4,355 tests passed. Live: a manual `--stages wire` run at 11:17 AM under the active cap stored MLB 6 / NFL 3 / NCAAF 2 items via the native fallback in 71 s; `host-health-latest.json` returned to `warn` (disk 8.7 GiB only) and the content-stages incident clears on the next operational report.

## Left for Adam

- **Codex usage.** Sol serves props (~75K tokens per MLB game), Hub lane reads, Wire grounded searches, Winners curation and prop selection. Two caps in 24 hours is the pattern to expect; nothing in routing was changed (founder decision). Options are yours: more credits, or moving one of the Sol consumers.
- **Curation retry.** Today's failed MLB curation runs used their two attempts; a new run starts when the candidate set changes (next published pick) and will succeed on Sol after 1:01 PM with the smaller record. If Adam prefers a re-run sooner, restart the worker after 1:01 PM.
- **Disk:** 8.7 GiB free on the production volume (warn).
- **NBA Wire** is skipped daily (no slate) by design.
