name: Daily Picks

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  daily:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Daily Picks
        run: |
          echo 'Run your daily picks script here'