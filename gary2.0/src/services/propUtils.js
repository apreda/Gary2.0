import { mlbStatsApiService } from './mlbStatsApiService.enhanced.js';
import { debugUtils } from '../utils/debugUtils.js';

export const propUtils = {
  formatMLBPlayerStats: async (homeTeam, awayTeam) => {
    // ... existing code from formatMLBPlayerStats ...
  },
  formatGameTime: (timeString) => {
    // ... existing code from formatGameTime ...
  },
  createPropPicksPrompt: (props, playerStats) => {
    // ... existing code from createPropPicksPrompt ...
  },
  parseOpenAIResponse: (response) => {
    // ... existing code from parseOpenAIResponse ...
  }
}; 