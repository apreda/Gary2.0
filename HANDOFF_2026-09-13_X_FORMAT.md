# September 13: X game-pick format

Adam explicitly corrected the image labels: **screenshot 1 is his preference throughout**. Use an opening factual sentence, a blank line, the bare pick, a blank line, and a second factual sentence. The Falcons/Bijan Robinson example is the requested layout. Do not restore the two-block Cardinals layout based on the earlier mixed-up image references. This is a founder preference, not a proven engagement uplift.

## Later founder correction: primary only, alert on failure

Adam rejected fallback behavior: failures must be visible promptly so the primary can be fixed. **This supersedes the v112 deterministic fallback described below.** Source `1cf8870a` removes that path, the hidden preselection copy filter and the internal model retry. The model now selects one pair ID via a forced tool call from code-validated pairs; it no longer copies text or calculates character length. The one chosen pair still passes final source validation before the fixed three-block format is assembled. Missing keys, timeout/transport failure, provider HTTP errors, invalid selection and missing valid source pairs stop that pick, with specific error codes. No alternate provider or copy is used. The primary call has a 25-second timeout. The historical two-block output was permitted by the old contract; no retained evidence proves that every two-block tweet was caused by a provider outage.

A degraded response is now HTTP 503 (unexpected outer errors remain 500), with structured health and per-pick diagnostics in pg_net receipts. A failed props read cannot be mistaken for a game without props; unavailable audience history stops selection instead of using priors. Metrics read/write failures surface too; the obsolete legacy-column write fallback was removed after confirming all three intent-metric columns exist. Genuine empty data and deliberate audience omissions remain healthy skips. Existing durable publication recovery is retained to reconcile confirmed receipts without duplicate sends.

The **Gary posting failures** heartbeat (`gary-posting-failures`) is active in Adam's current task at a five-minute interval. It runs the read-only `gary2.0/scripts/lib/socialFailureStatus.sql`, checks every retained scheduled failure as well as cron/response freshness and old unresolved receipts, and notifies only for new failures, changes, lost visibility or verified recovery. State belongs in `~/Library/Logs/Gary2.0/social-alert-state.json`. It never publishes or repairs by changing providers/copy. This is a local Codex monitor, not an instant cloud push guarantee: execution/notification depends on the host/app being available, and pg_net has roughly six hours of retention. The monitor query was tested against live healthy state and a read-only CTE fixture where a 503 failure was still detected despite a later 200 success. No synthetic database rows or tweets were created.

Verification before deployment: 163 focused tests, all 235 edge-helper tests, and Deno check passed. Actual bundled handler tests cover successful primary selection, missing key, HTTP 429, out-of-range pair ID, truncation, missing source pair, and HTTP 503 on read failure. Native/configuration WIP remains excluded.

Live verification: **social-auto-post v113**, deployed September 13 at 18:52:47.745 UTC, is ACTIVE with JWT enabled. The primary-only authenticated preview returned HTTP 200 and health `ok`, with a three-block Tigers hook. No tweet was sent. Evidence is in `docs/operations/social-format-2026-09-13/primary-only-preview.json` and `primary-only-production-truth.txt`. Production truth confirms edge timestamps and no unpushed source; its overall flag remains the preserved native/configuration working-tree entries.

## Earlier v112 delivery (historical)

Source `831f0b78` is committed and pushed on main. `social-auto-post` **v112** is active in `xuttubsfgdcjfgmskcol`, deployed September 13 at 18:42:01.697 UTC with JWT verification enabled. Future game-pick roots require both factual sentences in the model selection, retry, deterministic outage fallback, final validator, and unsent prepared-payload path. No test tweet was sent and no existing post was edited or deleted.

Both sentences still must be distinct, whole, standalone, concrete facts copied from the same supporting paragraph of Gary's original rationale, within the existing character budget. If no pair fits, the composer records `NO_SAFE_COPY` and skips that tweet; it does not invent filler, truncate facts or fall back to two blocks. The deterministic selector continues looking for a later valid pair when an earlier opening has no partner. The generic shared helper keeps its old optional-closing default for other consumers; this composer explicitly requires the closing. No prepared intents existed at the pre-deploy check. Already-sent publication recovery remains intact.

This changes the game-pick root format. It does not change pick generation, June's engine, rationale writing, the original facts, props replies, recaps, audience selection, cap, cadence or pregame windows. Current launch/Apple/model direction remains in the launch-review and Sunday-recovery handoffs.

## Verification and limits

- 137 focused Vitest cases pass, including actual bundled composer fixtures with the model unavailable, a single-fact rejection, and prepared-copy/cadence guards.
- All 235 edge-helper tests pass; Deno check passes.
- Live authenticated dry-run preview returned HTTP 200, health `ok`, and a three-block Tigers hook at 18:43:36 UTC. Preview deliberately ignores pregame timing, so its negative lead time is not a scheduled posting approval. No posting or metrics writes occurred. See `docs/operations/social-format-2026-09-13/preview.json`.
- Production truth confirms v112 deployment timestamp, no unpushed source, the correct scheduler folder and running scheduler/Winners workers, Sol games/MLB and Luna props. The sampled daily MLB store had 14/15 games published, zero started without a pick, and one pending. This is a point-in-time check, not complete NFL or historical results verification.
- Overall production truth remains flagged for the 11 pre-existing native/configuration working-tree entries, including the private Firebase plist. These are preserved and excluded from this commit. Edge checks compare timestamps, not deployed-source byte equality. Full output: `docs/operations/social-format-2026-09-13/production-truth.txt`.
